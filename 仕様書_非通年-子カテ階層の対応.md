# 仕様書：非通年「記入以外前回と同じ」の子カテ階層対応

- 対象ファイル：`sale処理-v52.xlsm` → **`sale処理-v53.xlsm`**
- 作成日：2026-08-03
- ステータス：**実装済み・検証合格**（2026-08-03）。問題1の実証まで完了。問題2の実証は未実施 → 6.3
- 前提：v51（親カテゴリ欠落）・v52（通年の階層判定統一）が適用済みであること

v52 完了後にアルゴリズム全体を検査して見つかった2件を扱う。**いずれも返答表の運用ルール（排他・重複記載なし）が守られていても発生する**構造的な問題。

---

## 1. 直すもの

| # | 内容 | 発症条件 | 現状 |
|---|---|---|---|
| **1** | 非通年「記入以外前回と同じ」に**子カテ階層が無い** | 子カテ単位に「記入以外前回と同じ」を指定 | 260731 では0件のため未発症 |
| **2** | `salegoods-前回と同じ-子カテ` の**結合キーにメーカー名が無い** | 子カテ単位に「前回と同じ」を指定 | 260731 では0件のため未発症 |

---

## 2. 問題1：非通年 記入以外に子カテ階層が無い

### 2.1 現象

`ファイル前処理-編集用-アップシート` はメーカー階層と親カテ階層しか Combine していない。

```m
追加されたクエリ1 = Table.Combine({#"名前が変更された列 ", #"salegoods-記入以外-メーカー"}),
追加されたクエリ2 = Table.Combine({追加されたクエリ1, #"salegoods-記入以外-親カテ"}),
                                                       ↑ 子カテ版が存在しない
```

子カテ単位に「記入以外前回と同じ」を指定すると:

| 経路 | 動作 |
|---|---|
| 前処理（記入があった商品） | ✅ 出力される（フォルダで選別しているため返答表の階層に依存しない） |
| sale 側（未記入の商品を salegoods で補完） | ❌ **出力されない**（メーカー階層にも親カテ階層にも一致しない） |

**「メーカーの商品すべてがセール対象」であるべきところ、記入があった商品だけがセールになる。**実質「返信あり」と同じ挙動に化ける。

### 2.2 他系統との比較

| 系統 | メーカー | 親カテ | 子カテ |
|---|---|---|---|
| 前回と同じ（非通年） | ✅ | ✅ | ✅（ただし問題2あり） |
| **記入以外前回と同じ（非通年）** | ✅ | ✅ | ❌ **これ** |
| 記入以外前回と同じ（通年） | ✅ | ✅ | ❌（v52 仕様書6章で対応しないと決定） |

**「前回と同じ」は3階層とも実装済み。**`salegoods-前回と同じ-子カテ` が goods.csv 経由でブランド品番から子カテゴリを引いており、これがそのまま手本になる。

### 2.3 通年側との違い（なぜ通年はこれで良いか）

v52 仕様書6章で「通年 sale 側の子カテ非対応」を制約として残したが、非通年とは事情が違う。

- 通年側は**メーカー行と子カテ行の混在**（＝ルール違反）でのみ問題化する
- 非通年側は**子カテ単位の指定だけで**（＝ルールどおりでも）欠落する

したがって非通年側は対応する。

---

## 3. 問題2：子カテ名はメーカーをまたいで重複する

### 3.1 現象

```m
// salegoods-前回と同じ-子カテ
マージされたクエリ数1 = Table.NestedJoin(#"展開された メーカーマスタ", {"goods.子カテゴリ"},
    #"セール返答表-前回と同じ-子カテ", {"子カテ"}, "…", JoinKind.Inner),
```

**子カテ名だけで結合している。**goods.csv を調べたところ、子カテゴリ名は一意ではなかった。

| 確認 | 結果 |
|---|---|
| 子カテゴリ総数 | 806 |
| **複数の親カテゴリにまたがる子カテゴリ** | **40件** |

```
GERRY            → AIM select / BNS select / GERRY / K SELECT / LEAD /
                   NAKASHIMA SELECT / RABBIT / Ree＆Mee / YMT select / shinsei（10親カテ）
THRASHER         → CQ SELECT / Ree＆Mee / sarai
OUTDOOR PRODUCTS → Ree＆Mee / offprice.ec
KRIFF MAYER      → KRIFF MAYER / Ree＆Mee
```

返答表で「メーカーA の GERRY を前回と同じ」と指定すると、**メーカーB〜J の GERRY まで巻き込んで出力される。**

### 3.2 結合キーの監査結果

返答表と結合している全12箇所を調べた。**キー不足はこの1本だけ**で、他はすべてメーカー名を含んでいた。

| クエリ | 左キー | 判定 |
|---|---|---|
| `salegoods-前回と同じ-親カテ` | メーカー名 + 親カテゴリ | ✅ |
| `salegoods-記入以外-親カテ` | 親カテゴリ + メーカー名 | ✅ |
| `通年-記入以外-前処理` / `通年-返信あり-前処理` | メーカー名 + ブランド | ✅（v52 で統一済み） |
| **`salegoods-前回と同じ-子カテ`** | **子カテのみ** | ❌ |

---

## 4. 決定事項

| # | 決定 | 根拠 |
|---|---|---|
| 1 | 非通年「記入以外前回と同じ」に子カテ階層を追加する | 問題1。ルールどおりの運用で欠落する |
| 2 | 子カテの結合キーは **メーカー名 + 子カテ** の2キーにする | 問題2。子カテ名は40件が重複しており一意でない |
| 3 | 子カテゴリの取得は **goods.csv 経由**（ブランド品番 → 子カテゴリ） | `salegoods-前回と同じ-子カテ` と同じ手法。salegoods に子カテ列が無いため |
| 4 | 通年側の子カテ対応は**引き続き行わない** | v52 仕様書6章の判断を維持。発症条件がルール違反に限られる |
| 5 | フォルダと返答表の二重管理は**今回触らない** | kintone 移行時に「フォルダ廃止・返答表に一本化」として対応（第7章） |

---

## 5. 変更内容

Power Query のみ。VBA・シート構成・設定シートは変更しない。

### 5.1 新規クエリ①：`セール返答表-記入以外-子カテ`

既存の `セール返答表-前回と同じ-子カテ` と対になるもの。

```m
let
    ソース = セール返答表,
    フィルターされた行 = Table.SelectRows(ソース, each ([親カテ] = null) and ([子カテ] <> null) and ([最新] = "記入以外前回と同じ"))
in
    フィルターされた行
```

### 5.2 新規クエリ②：`salegoods-記入以外-子カテ`

`salegoods-前回と同じ-子カテ` の子カテ引きと、`salegoods-記入以外-親カテ` の出力形を組み合わせる。**結合キーはメーカー名 + 子カテの2キー**（決定事項2）。

```m
let
    ソース = salegoods,
    Jメーカーマスタ = Table.NestedJoin(ソース, {"親カテゴリ"}, メーカーマスタ, {"親カテゴリ名"}, "メーカーマスタ", JoinKind.LeftOuter),
    Eメーカーマスタ = Table.ExpandTableColumn(Jメーカーマスタ, "メーカーマスタ", {"メーカー名"}, {"メーカーマスタ.メーカー名"}),
    Jgoods = Table.NestedJoin(Eメーカーマスタ, {"ブランド品番"}, goods, {"ブランド品番"}, "goods", JoinKind.LeftOuter),
    Egoods = Table.ExpandTableColumn(Jgoods, "goods", {"子カテゴリ"}, {"goods.子カテゴリ"}),
    J返答表 = Table.NestedJoin(Egoods, {"メーカーマスタ.メーカー名", "goods.子カテゴリ"}, #"セール返答表-記入以外-子カテ", {"メーカー名", "子カテ"}, "返答表", JoinKind.Inner),
    記入済みを除外 = Table.NestedJoin(J返答表, {"ブランド品番"}, #"ファイル前処理-編集用ーブランド品番", {"ブランド品番"}, "前処理品番", JoinKind.LeftAnti),
    削除された他の列 = Table.SelectColumns(記入済みを除外,{"親カテゴリ", "ブランド品番", "変更後セール価格（税抜）"}),
    追加されたカスタム = Table.AddColumn(削除された他の列, "要確認", each null),
    追加されたカスタム1 = Table.AddColumn(追加されたカスタム, "新オフ率", each null),
    #"名前が変更された列 " = Table.RenameColumns(追加されたカスタム1,{{"変更後セール価格（税抜）", "変更後セール価格"}})
in
    #"名前が変更された列 "
```

`goods` はブランド品番ごとに複数行（商品コード単位）あるため、**`Table.Distinct(Egoods, {"ブランド品番"})` を挟んで行の増殖を防いでいる**（実装時に追加）。

なお既存の `salegoods-前回と同じ-子カテ` にはこのガードが無い。現在は `セール返答表-前回と同じ-子カテ` が0件のため発症しないが、**子カテ単位に「前回と同じ」を指定すると行が増える可能性がある**。次に子カテ運用が発生する前に同じ Distinct を入れること（未対応）。

**「接続の作成のみ」にすること**（v51 仕様書 10.6）。

### 5.3 `ファイル前処理-編集用-アップシート` に1行追加

```m
// 変更前
    追加されたクエリ2 = Table.Combine({追加されたクエリ1, #"salegoods-記入以外-親カテ"}),

// 変更後（1ステップ追加）
    追加されたクエリ2 = Table.Combine({追加されたクエリ1, #"salegoods-記入以外-親カテ"}),
    追加されたクエリ3 = Table.Combine({追加されたクエリ2, #"salegoods-記入以外-子カテ"}),
```

**後続ステップが `追加されたクエリ2` を参照しているので、参照先を `追加されたクエリ3` に付け替えること。**

### 5.4 `salegoods-前回と同じ-子カテ` の結合キーを直す

```m
// 変更前
    マージされたクエリ数1 = Table.NestedJoin(#"展開された メーカーマスタ", {"goods.子カテゴリ"},
        #"セール返答表-前回と同じ-子カテ", {"子カテ"}, "セール返答表-前回と同じ-親カテ", JoinKind.Inner),

// 変更後
    マージされたクエリ数1 = Table.NestedJoin(#"展開された メーカーマスタ", {"メーカーマスタ.メーカー名", "goods.子カテゴリ"},
        #"セール返答表-前回と同じ-子カテ", {"メーカー名", "子カテ"}, "セール返答表-前回と同じ-親カテ", JoinKind.Inner),
```

ステップ名は変更しない（後続が参照しているため）。

### 5.5 変更しないもの

- 通年側すべて（v52 で対応済み。子カテ非対応は制約として維持）
- `ファイル前処理-編集用`（フォルダで選別する仕組みは維持）
- `返信あり` 系統、`追加セール` 系統
- 全 VBA モジュール

---

## 6. 検証

### 6.1 出力が変わらないことの確認

260731 には子カテ単位の「記入以外前回と同じ」も「前回と同じ」も**0件**（子カテ行は 51 グルービーの2件のみで、いずれも「返信あり」）。したがって**出力は1行も変わらないはず**。

v52（2026-08-03 13:26 更新、MD5 `DB8A696B44804A8BF82AD1A19AD5D416`）の実測値:

| シート | 行数 |
|---|---|
| 前回と同じ | 9,795 |
| 記入以外ー前処理 | 875 |
| **記入以外前回と同じ** | **4,974** |
| 返信あり-前処理 / 返信あり | 6,032 / 5,932 |
| 通年-前回と同じ | 122 |
| 通年-記入以外-前処理 / 通年-記入以外前回と同じ | 75 / 2,586 |
| 通年-返信あり-前処理 / 通年-返信あり | 84 / 84 |

全22シートの行単位 diff で **追加0行 / 削除0行**を確認する。

**実施結果（2026-08-03）：合格。**`sale処理-v53.xlsm`（MD5 `69D82207EFF65162FC887BCF96F777E5`）を v52 と突き合わせ、
**全22シートで追加0行 / 削除0行**。列構成も変わらず1セルの差分も無し。シート数も22のまま
（新規2クエリは接続専用）。`salegoods-記入以外-子カテ` の goods 結合による行の増殖も無い。

### 6.2 問題1が直ったことの確認

返答表のコピーで子カテ単位の指定を作る。非通年のメーカーを使う。

1. 非通年で「記入以外前回と同じ」のメーカー（例：`68 グロリア` / 親カテ `Gloria` 1,026件）を選ぶ
2. そのメーカー行のG列を空にし、**子カテ行**を追加（`A=メーカー名 / C=子カテ名 / G=記入以外前回と同じ`）
3. すべて更新

| | v52（修正前） | v53（修正後） |
|---|---|---|
| その子カテの**記入があった商品** | 出力される | 出力される |
| その子カテの**未記入の商品**（salegoods 補完） | **出力されない** | **出力される** |

#### 実施結果（2026-08-03）：**合格**

**注意：このテストは価格設定リストの分割粒度をあえて崩している**（7.0 参照）。
Gloria のリストはメーカー単位で1本にまとまっているため、前処理側に指定外の
`page one` 96件が残る。実運用では子カテ指定ならリストも子カテ単位で分けるので発生しない。
ここで見るのは **sale 側（salegoods 補完）が子カテ単位で正しく効くか**の1点。

`68 グロリア`（親カテ `Gloria`）を使用。`_テスト用\◎セール返答表_子カテテスト.xlsm` で
**G82（メーカー行）を空にし、288行目に子カテ行を追加**（`A=68 グロリア / C=jou jou lier / G=記入以外前回と同じ`）。

選定理由：`jou jou lier` は設定リスト129件のうち記入が1件だけ、salegoods は255件。
補完が効くかどうかが件数にはっきり出る。

| 確認項目 | **v52** | **v53** | 判定 |
|---|---|---|---|
| Gloria 合計 | **97** | **351** | ✅ |
| └ 前処理（記入分） | 97 | 97 | 変化なし |
| └ **salegoods 補完** | **0** | **254** | ✅ 子カテ階層が効いた |
| 補完分の子カテ内訳 | — | **`jou jou lier` のみ 254件** | ✅ 他ブランドの混入なし |
| ブランド品番の重複 | — | **0件** | ✅ 行の増殖なし |
| 記入以外前回と同じ 総行数 | 4,045 | **4,299** | 予測どおり |

254 = salegoods の `jou jou lier` 255件 − 記入済み1件（LeftAnti で除外）。

**v52 では記入した97件だけがセールになっていた。**「メーカーの商品すべてがセール対象」であるべき
ところが実質「返信あり」に化けており、狙った症状がそのまま再現された。

同じ Gloria 配下の `page one`（salegoods 559件）が1件も混ざっていないことから、
goods.csv 経由の子カテ判定が正確に効いていることも確認できた。

なお非通年の前処理はフォルダ由来のため、メーカー行を空にしても記入分97件は残る
（`記入以外ー前処理` は875行のまま変化なし）。通年側との違いがここに現れている。

### 6.3 問題2が直ったことの確認

子カテ名が重複しているもの（`GERRY` など40件のいずれか）を使う。

1. 返答表で `A=Ree＆Mee のメーカー名 / C=GERRY / G=前回と同じ` を設定
2. すべて更新

| | v52（修正前） | v53（修正後） |
|---|---|---|
| 指定したメーカーの GERRY | 出力される | 出力される |
| **他メーカー（AIM select 等）の GERRY** | **出力される（誤り）** | **出力されない** |

### 6.4 実施後

返答表のコピーを使うこと。本番の返答表・提出ファイルは変更しない。

---

## 7. 対応しないもの

| # | 内容 | 理由 |
|---|---|---|
| 1 | 通年 sale 側の子カテ階層 | v52 仕様書6章。発症条件がルール違反に限られ、重複チェックマクロで検出できる |
| 2 | **フォルダと返答表の二重管理** | 下記 7.1 |
| 3 | 在庫0の商品がセール対象にならない件 | 仕様どおり。下記 7.2 |

### 7.0 価格設定リストの分割粒度が前提になっている（2026-08-03 追記）

非通年の前処理はフォルダを丸ごと読み、返答表の階層を見ない。それでも指定外カテゴリが
混ざらないのは、**価格設定リストの分割粒度が返答表の指定粒度と一致しているから**（ユーザー確認済み）。

| 返答表の指定 | 価格設定リスト | 実例（260731） |
|---|---|---|
| メーカー単位 | メーカーごとに1本 | `00068-グロリア株式会社様…xlsx`（Gloria 全ブランド641件） |
| 子カテ単位 | **子カテごとに分けて送る** | `00051-…グルービー様…_BF2 select.xlsx`（BF2 select 50件のみ）<br>`コピー00051-…_SmileLife.xlsx`（SmileLife 34件のみ） |

6.2 のテストで `page one` 96件が漏れたのは、**メーカー単位で1本にまとまっている Gloria の
リストに対して、返答表だけ子カテ指定にした**ため。実運用ではありえない組み合わせだった。

したがって**これは不具合ではない。**粒度の一致は、返答表の排他ルールと同じ「守られている前提」に含まれる。

### 7.1 フォルダと返答表の二重管理（kintone 移行時に対応）

非通年は**フォルダ**（【記入以外前回と同じ】【返信あり】等）で系統を判定し、通年は**セール返答表**で判定している。同じ情報が2箇所にあり、一致の保証が無い。

経緯は、以前の返答表に列H「通年セール」が無く指定できなかったため、やむを得ずフォルダで分けたのが始まり（ユーザー確認済み）。

**急を要しない**と判断した理由:

- 食い違っても静かに誤った価格が出るのではなく、件数の異常として現れる（前処理は出るのに補完されない等）
- 歴史的経緯で残った構造を後方互換を保ちつつ直すのは割に合わない
- **kintone 移行**（返答表・設定リストの仕様変更、品番/メーカー情報を kintone から取得、重い CSV からの脱却）が予定されており、フォルダ分けの是非はその設計の中で決めるべき

**移行時の要件として次を残す。**

> セール返答表を系統判定の唯一の権威とし、提出フォルダによる分類を廃止する。
> 非通年の前処理（`ファイル前処理-編集用`、`返信あり-前処理`）も、通年側と同じ
> 3階層判定（子カテ > 親カテ > メーカー）で処理方法を決めるようにする。

なお `goods.csv` は124MB あり、「すべて更新」に数分かかる。kintone 化はこの面でも効果が見込める。

### 7.2 在庫0の商品

商品マスタにあるのに、セール価格設定リストにも salegoods にも無い商品がある（MSA select で144件）。調査の結果:

| 確認 | 結果 |
|---|---|
| セール価格設定リスト掲載 1,061件のうち在庫・販売可能数がともに0/空 | **0件** |
| 未カバー144件の登録日 | 2020/03〜2025/05 に分散（新商品ではない） |
| 未カバー144件の Web表示 | 142件が「1」（カバー済と同傾向） |

**セール価格設定リストは在庫のある商品だけで生成されている。**リストに載らない商品は在庫0とみて整合する。在庫0の商品はセールのしようがないため、出力されないのが正しい挙動。**問題なし。**
