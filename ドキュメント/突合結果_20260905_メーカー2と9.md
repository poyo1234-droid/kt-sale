# 突合結果｜2026-09-05 メーカー2 / メーカー9

在庫分析の洗い替えと、売上の絞り込み期間の回避（テスト用レコードで前回セール期間を指定）を
行ったうえで再突合した。**属性の不一致は両社とも0件。**
実行手順は `作業手順_kintone-セール価格設定リスト出力と突合.md`、仕様は
`仕様書_セール価格設定リスト_データ仕様.md` を参照。

## 実行条件

- セール設定 k/33 のテスト用レコード（`0801-test` / 2026-08-01〜2026-08-07）から実行
  - 本来の対象期間は 20260808〜20260821 だが、売上の絞り込みが今回セール期間になっている
    不具合（§12-3-4）を回避するため、**前回セール期間**を指定した
- 在庫分析 k/65 は 0803 のファイルで洗い替え済み（列の入れ替わりを解消。§12-3-2）
- 商品マスタ「販売中の商品」ビューは対象メーカーに絞って実行
- 突合は `検証スクリプト\キントーン突合.py`（健全性チェック追加版）

## 結果

| | メーカー2 スタンレーインターナショナル | メーカー9 ガレージ |
|---|---|---|
| キントーン | 832行（在庫あり 113） | 153行（在庫あり 62） |
| 旧システム | 113行 | 62行 |
| 共通 | 113 | 62 |
| キントーンのみ | 719 | 91 |
| 旧のみ | 0 | 0 |
| **属性の不一致** | **0件** | **0件** |
| 実績の不一致 | 409件 | 188件 |

**在庫あり＝旧の行数＝共通** が両社で一致した。在庫>0 で絞れば母集団が揃うことを2社で確認。

### 9/4 との比較（メーカー2）

| | 9/4 | 9/5 |
|---|---|---|
| 属性の不一致 | 45 | **0** |
| 実績の不一致 | 411 | 409 |
| うち実質の残差 | 2 | **0** |
| 健全性チェックの警告 | 7件 | 5件 |

減った実績2件は `MEI-000-205014` の受注数・売上額。**売上の絞り込み期間を前回セールに
合わせたら旧システムと一致した**（受注数1／売上額6,320）。診断が正しかったことの確認になる。

消えた警告は「受注数が全行ゼロ」と「プロパー価格が値下げ後の疑い」。

## 実績409件の内訳（メーカー2）

| 項目 | 件数 | 正体 |
|---|---|---|
| 販売開始日 | 113 | 表記差（旧 `2026/8/1 8` / k `2026-08-01`）。日付は同じ。突合対象外 |
| 直近7日販売数 | 109 | 0が空欄 |
| 直近30日販売数 | 104 | 0が空欄 |
| お気に入り登録数 | 75 | 0が空欄 |
| 直近セール価格(税抜) | 4 | 0が空欄 |
| 直近セールオフ率 | 4 | 0が空欄 |

下5つの合計が **296件** で、`|| ''` が未修正であることによるノイズ。

**販売開始日を除き、「0と空欄の組み合わせ」以外の不一致は0件。**
中身が本当に違う値は1つも残っていない。**実質の残差はゼロ。**

## 残る警告（健全性チェック）

| 警告 | 扱い |
|---|---|
| 「オフ率」が共通全件で両方とも空欄 | メーカーが記入する欄。仕様どおり |
| 「受注数」「売上額」が中身のない一致 | 実績のある品番が少ないため。異常ではない |
| オートフィルタ範囲がデータ最終行と合わない | 未対応（§9-4） |
| データ最終行より下に行が残っている | 未対応（§9-4） |

## 数字に出なかった問題

**突合の数字が良好でも、宛先まわりは検証できていない。**メーカー9で実行して初めて発覚した。

- セール返答表 k/60 の連絡先3列が**すべて空**だった（メーカー2も同様）
- 親カテゴリの連絡先を設定したところ、今度は `[400] CB_IJ01 不正なJSON文字列です` で
  一括登録が落ちた。`FA様担当者` が `USER_SELECT` なのに文字列を渡しているため（§12-3-5）
- メーカー9は `セールリスト出力単位 = 親カテゴリ` に設定され、親カテゴリ7つのうち4つに
  別々の宛先が登録されているのに、**出力は1ファイルだけ**だった。ログも `(1/1)`

詳細と設計は §11-7 / §12-3-5。**再突合のたびに k/60 のレコードを目視すること。**

## 次にやること

1. 差し戻し 1〜9 をエンジニアへ（§12-3）
2. 修正後にメーカー2・9で再突合。目標は **キントーンのみ0件 / 実績113件（表記差のみ）**
3. 分割メーカー（9 / 19 / 51 / 121）でファイル分割のコードパスを確認
4. 「販売中の商品」ビューの絞り込みを外して全メーカーへ展開
