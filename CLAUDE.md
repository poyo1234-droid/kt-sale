# CLAUDE.md

このフォルダで作業する際の前提情報。

## プロジェクト概要

ZOZOTOWN のセール処理を行う Excel マクロブック `sale処理-v50.xlsm` の保守・改修。
メーカーから提出されたセール価格ファイルを Power Query で集約し、ZOZO へアップロードする TSV/TXT を VBA で出力する。

- **VBA**: 12モジュール（出力マクロ、集計、クエリ更新、重複チェック）
- **Power Query**: 約90クエリ（`Section1.m`）
- ブック本体は NAS 上（`T:` = `\\dop\ドキュメント`）

## 現在の状況（2026-09-01 時点）

**最新は `sale処理-v53.xlsm`（無印・NAS）と `sale処理-v53F.xlsm`（F版・取引先 Dropbox）。**
v51・v52・v53 とも自社環境（NAS / 260731 データ）で検証合格。
**旧版 v50〜v52 は `OLD\` に移動した**（git 履歴は保持）。

### キントーン移行の突合を開始（2026-09-01 / 2026-09-02 訂正）

セール価格設定リストの生成が**キントーンへ移る**ため、両者の出力を突き合わせるスクリプトを追加した。
`検証スクリプト/キントーン突合.py`（詳細は `ドキュメント/仕様書_キントーン移行-セール価格設定リストの突合.md`）。

**ここでの「旧システム」は SQL をメインにした基幹システムを指す。**リストを生成していたのはこの基幹
システムであり、`sale処理-vNN.xlsm` ではない。`sale処理` は提出されたリストを受けて ZOZO 用 TSV を出す
**下流の別ツール**で、突合の対象ではない。

メーカー2・4 の2件で試行した結果、**属性10項目のうち9項目が221件すべて一致**。
移行ロジックそのものは正しく動いている。ただし**要確認8件**を検出した。要点のみ:

- **実績9項目が1,163件不一致**：在庫・販売数・お気に入り・受注数・売上額・直近セール実績・販売開始日。
  **両者とも同じ回（`20260207～20260220`）が基準**で、キントーンは 9/1 に実行しただけなので、
  これは実データの差＝抽出条件の問題。在庫から順に詰める（仕様書 7章⑦）
- **親カテゴリ28件の不一致**：すべて 旧[SI select]→キントーン[MEI]。ZOZO 在庫分析と照合すると**キントーン側が正しい**。旧がメーカー単位で親カテを固定していた疑い
- **旧システムのみ2件**（`CAS-000-231002` / `CAS-000-231003`）：廃番か移行漏れか

**2026-09-02 のユーザー確認で 8件中4件が決着し、新規1件を検出した。**

| 決着 | 内容 |
|---|---|
| 在庫0の扱い | **アーカイブ商品を対象外にする**。在庫の有無ではなくアーカイブ状態で絞る |
| 欠落12項目 | **社内会議で不要と判断済み**。対応不要 |
| 販売開始日 | **不具合ではない。名称の問題。**ZOZO がセール開始（＝価格変更）で上書きするため、実態は「直近のセールが始まった日」。旧の `2026/2/7 8:45` は同じ回の salegoods のセール開始予定日時 20,059件と一致。**「前回セール実施日」等へ改名する**。この列は実行日に依存するので突合対象から外す |
| ファイル名の期間表記 | **同じ回を指すことをデータで確認**（メーカー2の直近セール価格が137/138一致）。意図的な表記なら問題なし |

**新規 ⑨：直近セール実績の紐付けがメーカーによって壊れている。**
直近セール価格がキントーンと旧システムで、メーカー2は 137/138 一致なのに**メーカー4は 11/83**。
うち31件はキントーン側が空欄（元データの `salegoods.csv` には値がある）。違う値・元データに無い値も出る。
オフ率も同件数ずれる。**`salegoods.csv` の結合キー（メーカー / 親カテ / 子カテ）を疑う** — v51〜v53 で
実際に起きた不具合と同じ形。仕様書 7章⑨。

**2026-09-02 時点で商品マスタを全面更新中のため、突合は保留。**更新前の出力と突き合わせても
行の顔ぶれも値も変わるため、更新完了後にキントーン出力を取り直してから再開する。

### v53：非通年「記入以外前回と同じ」の子カテ階層対応（`ドキュメント/仕様書_非通年-子カテ階層の対応.md`）

新規2クエリ（`セール返答表-記入以外-子カテ` / `salegoods-記入以外-子カテ`）を追加し、
`ファイル前処理-編集用-アップシート` に Combine を1ステップ追加。あわせて
`salegoods-前回と同じ-子カテ` の結合キーを **メーカー名 + 子カテ** の2キーに修正。

- 出力不変：全22シートで追加0行 / 削除0行
- 問題1（子カテ階層の欠落）：Gloria `jou jou lier` の salegoods 補完が 0 → 254件、他ブランドの混入なし
- 問題2（子カテ名の重複）：GERRY で発生していた品番重複 17種 → 0種。実害は「他メーカーが出る」ではなく**同一品番の二重出力**だった

**通年 sale 側の子カテ非対応は引き続き制約として維持**（v52 仕様書6章）。

### 担当者の本番出力との突き合わせ（2026-08-04）：**51ファイル全て MD5 一致**

260731 回に担当者が ZOZO へアップロードした TXT（`260731-org\…\TXT_output今野様\`）と、
v53 で出力した TXT が**7系統・51ファイルすべてバイト単位で一致**。
v51〜v53 の改修が本番出力を1バイトも変えていないことを実データで確認した。詳細は v53 仕様書 6.5。

**あわせて判明：親カテゴリ欠落バグは本番データには現れていなかった。**
担当者が毎回「セール価格設定リストに当該親カテの商品を仮登録 → 更新 → 出力 → 仮登録を削除」という
手作業で回避していたため（ユーザー確認済み）。v51 仕様書 0.4 のベースライン 2,271 行は、
その手作業の**前**に保存されたキャッシュ値であって本番の出力ではない。
**v51 の実際の価値は、この毎回の手作業を不要にしたこと。**

### v52：通年側の階層判定を統一（`ドキュメント/仕様書_通年-階層優先順位の統一.md`）

判定用クエリ3本を新設し、優先順位を **子カテ > 親カテ > メーカー** に統一。3つの欠陥（親カテ階層を見ていない／記入以外側と返信あり側で順序が逆／①が親カテ行を無視）を解消。実データで実証済み。今のデータでは出力不変。

**既知の制約1件**（sale 側が子カテ階層を見ない）は対応せず記録。排他ルール違反時のみ発生し、重複チェックマクロで検出できる。仕様書6章。

### v51：親カテゴリ欠落バグの修正

- 新規3クエリ（①メーカー階層 ②親カテ階層 ③通年フィルタ付き統合）を追加し、`通年-記入以外前回と同じーsale` の Inner Join 参照先を1行差し替え
- EVER METAL 0→192 / BLACK JACKALL CROWN 0→123、既存3カテゴリは1行も変動なし（総行数 2,271→2,586）
- 全22シートの行単位 diff で「追加315行 / 削除0行」、VBA は全モジュール無変更
- VBA「ブランド品番重複チェック」も実行し **重複なし**（2026-08-03）

詳細は `ドキュメント/仕様書_通年-記入以外前回と同じ_親カテゴリ欠落対応.md` を参照。

**再開時は v53 の仕様書（`ドキュメント/仕様書_非通年-子カテ階層の対応.md`）から読むこと。**

### v53F：F版の作成（2026-08-04）

`sale処理-v53F.xlsm` を作成。**設定シートの C4 / B6 / B7 の3セルのみ書き換え**、
Power Query・VBA・他シートは完全に無変更（MD5 一致で確認）。
**取引先環境での実行確認は未着手。**

- B5・B8〜B15 は `=$C$4&Cn` の数式で **C4 に連動する**ため触らない
- **C16 は `=LEFT(CELL("filename",A2),…)` ＝ ブック自身の場所を自動取得する数式**。
  Power Query からも VBA からも参照されておらず、**書き換えてはいけない**
  （作業手順書に「C16 も書き換える」と誤記があったため訂正済み）

### 次にやること

0. **商品マスタの全面更新の完了を待つ。**完了後にキントーン出力を取り直し、残りメーカー
   （旧側 `送信\` に186ファイル・メーカー実数175）を揃えて `python キントーン突合.py` を引数なしで実行。
   そのうえで **7章⑨（直近セール実績の紐付け）を最優先**で調べ、続けて実績項目を在庫から順に詰める
1. **F版を取引先環境で確認** — `ドキュメント/作業手順_v51_親カテゴリ欠落修正.md` フェーズ2 手順2-3 以降。
   `sale処理-v53F.xlsm` は作成済みなので、Dropbox 環境に置いて「すべて更新」→ 検証
2. v51 仕様書8章の残りの既知課題（`ファイル前処理-編集用-親カテ抽出` の親カテ欠落、Module3/4 のバグ、出力マクロの性能）

`25 nobu` による親カテ単位（②）のテストは 2026-08-03 に実施し合格（仕様書 10.7）。

## ファイル構成

| ファイル | 内容 |
|---|---|
```
エクセル開発/
  CLAUDE.md                     ← 直下必須（セッション開始時に自動で読まれる）
  .gitignore                    ← 直下必須
  sale処理-v53.xlsm              ← 現行ブック（無印・NAS）
  sale処理-v53F.xlsm             ← 現行ブック（F版・取引先 Dropbox）
  saleファイルチェックーv9.xlsx
  ドキュメント/                  ← 仕様書・手順書・設定値
  検証スクリプト/                ← TXT の出力と突き合わせ
  OLD/                          ← 旧版ブック（v50〜v52）
  _テスト用/                     ← git 追跡外
```

| ファイル | 内容 |
|---|---|
| `sale処理-v53.xlsm` | **最新（無印・NAS）**。非通年の子カテ階層に対応（全検証合格） |
| `sale処理-v53F.xlsm` | **最新（F版・取引先 Dropbox）**。設定シート3セルのみ差し替え。取引先環境での確認は未実施 |
| `saleファイルチェックーv9.xlsx` | ファイルチェック用 |
| `ドキュメント/仕様書_通年-記入以外前回と同じ_親カテゴリ欠落対応.md` | 調査結果と修正仕様（v51・完了） |
| `ドキュメント/仕様書_通年-階層優先順位の統一.md` | 階層判定を 子カテ>親カテ>メーカー に統一（v52・**実装済み／検証合格**） |
| `ドキュメント/仕様書_非通年-子カテ階層の対応.md` | 非通年 記入以外の子カテ階層追加＋結合キー修正（v53・**実装済み／全検証合格**） |
| `ドキュメント/作業手順_v51_親カテゴリ欠落修正.md` | 実作業の手順書。フェーズ2 が F版の作成・確認 |
| `ドキュメント/設定シート_パス貼付用_260731.md` | 設定シートのセル構造と貼付値（NAS 版 / F版） |
| `ドキュメント/仕様書_キントーン移行-セール価格設定リストの突合.md` | キントーン出力との突合仕様と、初回実行で判明した要確認8件 |
| `ドキュメント/_v52貼付用.txt` / `_v53貼付用.txt` | 実装時の M コード貼付シート |
| `検証スクリプト/` | TXT の出力と突き合わせ（バイト / 文字 / 列単位）＋ キントーン突合。使い方は同フォルダの README |
| `OLD/sale処理-v50.xlsm` | 調査時点の本体（無変更で保持）。実体は **F版** — v51 仕様書 第9章参照 |
| `OLD/sale処理-v51.xlsm` | 親カテゴリ欠落を修正（検証合格） |
| `OLD/sale処理-v52.xlsm` | 通年の階層判定を統一（検証合格） |

### 無印版と F版

`sale処理-vNN.xlsm`（無印）と `vNNF.xlsm`（F付き）は、**設定シートのパスだけが違う別環境向け**のファイル。Power Query・VBA はほぼ同一。

| | 無印（自社 NAS） | F付き（取引先 Dropbox） |
|---|---|---|
| パス | `T:\…` / `\\dop\ドキュメント\…` | `C:\Users\user\Dropbox\…` |
| マスタ | 作業フォルダ内に全部コピー | Dropbox の別ツリーを参照 |

判別は設定シート C4 の先頭を見る。**同じ名前で保存しないこと**（260731 で取り違えが発生し、無印版が失われている）。

### 検証用データの場所

`..\260731\セール情報設定リスト202608019～202608079\セール情報設定リスト202608019～202608079\` に
`goods.csv`（124MB）、`salegoods.csv`、`売上目標実績2025.xlsx`、各提出フォルダが揃っている。
`◎セール返答表.xlsm` のみ `..\260731\◎セール返答表\` 配下。

**設定シートを NAS パスにすれば、この環境で更新・検証できる。**

## ブックの中身を調べる方法

xlsm を直接開かなくても、Python + oletools で中身を読める。**調査は必ずこの方法で行い、ブックを書き換えないこと。**

```bash
# 1. xlsm を zip として展開
Copy-Item sale処理-v50.xlsm sale.zip
Expand-Archive sale.zip -DestinationPath unz
```

**VBA の抽出**

```python
from oletools.olevba import VBA_Parser
p = VBA_Parser(r"unz\xl\vbaProject.bin")
for (fn, sn, vn, code) in p.extract_macros():
    print(vn, code)
```

**Power Query（M コード）の抽出**

```python
import re, base64, struct, zipfile, io
raw = open(r"unz\customXml\item1.xml", "rb").read().decode("utf-16")
m = re.search(r">([A-Za-z0-9+/=\s]+)</DataMashup>", raw)
b = base64.b64decode(m.group(1))
ver, plen = struct.unpack("<II", b[0:8])   # 先頭8バイトはヘッダー
z = zipfile.ZipFile(io.BytesIO(b[8:8+plen]))
z.extractall("pq")                          # pq/Formulas/Section1.m が全クエリのソース
```

**シートデータの確認**

`openpyxl.load_workbook(path, data_only=True, read_only=True)` で値を読む。

## 処理の全体像

```
メーカー提出ファイル（フォルダ別）      外部マスタ
  ├ 【記入以外前回と同じ】              ├ salegoods.csv  … 前回のセール価格
  ├ 【返信あり】                        ├ goods.csv      … 商品マスタ（子カテゴリを持つ）
  ├ 【通年セール】                      ├ メーカーマスタ … 親カテゴリ→メーカー名
  └ 【追加セール】                      └ ◎セール返答表  … メーカーごとのセール方針
        │                                      │
        └──→ fxファイル前処理 ←───────────────┘
                    │
              各シート（前処理／出力）
                    │
              VBA で TSV/TXT 出力 → ZOZO へアップロード
```

### 処理系統の意味（ユーザー確認済み・2026-08-03）

**セール価格設定リスト**（`00025-…様 セール価格設定リスト20260801～20260807.xlsx` 等）は
**毎回すべての取引先にメールしている**。系統の違いは、その後どうなったか。

そして**セール返答表は、どの系統でも担当者が設定する**。メーカーからの回答を担当者が記録する台帳であり、
返送の有無や回答の形（リスト返送／メールで一言／親カテ単位の指示）に関わらず、最後は担当者が埋める。
非表示行と担当者欄の「（保留）」、階層指定の排他ルール、前回の値の消し忘れ（34 湯浅札幌）は
いずれもこの入力作業の産物であり、だからこそ実装側にガードが要る。

| 系統 | リストの返送 | salegoods（前回価格） | セール対象 |
|---|---|---|---|
| **前回と同じ** | **返ってこない**（「前回と同じでお願いします」と回答があるだけ） | 全部これ | メーカーの商品**すべて** |
| **記入以外前回と同じ** | 返ってくる（価格を変える分だけ記入されている） | 未記入の商品を補完 | メーカーの商品**すべて** |
| **返信あり** | 返ってくる | **使わない** | **記入があった商品だけ** |

- 「前回と同じ」は返送が無いだけで、依頼はしている。提出フォルダに該当メーカーのファイルが
  無いのは**依頼していないからではなく、返ってこないから**
- **「記入以外前回と同じ」と「返信あり」の違いは、返送されたリストの未記入行の扱い。**
  前者は未記入分も前回価格でセール対象にする。後者は未記入分をセール対象にしない
- したがって**「返信あり」に salegoods 補完を入れてはいけない**。セール対象でない商品に
  価格が付いてしまう
- `Ptuunen-p` / `Ptuunen-zenkai` は通年セール価格の一覧であって、セール価格設定リストではない

この違いを踏まえないと除外処理の設計を誤る（v52 の検討時に実際に誤りかけた）。

### 価格設定リストの分割粒度（ユーザー確認済み・2026-08-03）

**セール価格設定リストの分割粒度は、セール返答表の指定粒度と一致させる。**

| 返答表の指定 | 価格設定リスト |
|---|---|
| メーカー単位 | メーカーごとに1本 |
| 親カテ単位 | 親カテごとに分けて送る |
| 子カテ単位 | **子カテごとに分けて送る** |

実例（260731 の【通年セール】フォルダ）:

```
00051-…グルービー様 セール価格設定リスト…_BF2 select.xlsx   → BF2 select 50件のみ
コピー00051-…グルービー様 セール価格設定リスト…_SmileLife.xlsx → SmileLife 34件のみ
```

`51 グルービー` は返答表で子カテ単位（BF2 select / SmileLife）に指定しており、
リストもその粒度で分かれている。一方 `68 グロリア` はメーカー単位の指定なので1本にまとまっている。

**非通年の前処理（`ファイル前処理-編集用`、`返信あり-前処理`）はフォルダを丸ごと読み、
返答表の階層を見ない。**指定外カテゴリが混ざらないのは、この粒度一致が守られているため。

粒度が食い違うと（例：メーカー単位の1本のリストに対して返答表は子カテ単位）、
**指定していないカテゴリの記入行まで出力される。**実験で確認済み（Gloria で page one 96件が漏れた）。
ただしこれは運用ルール違反にあたる状態であり、通常は発生しない。

### 重要な構造

**セール返答表は メーカー / 親カテ / 子カテ の3階層**で指示を出す。各階層に抽出クエリがある（`セール返答表-記入以外-メーカー`、`セール返答表-記入以外-親カテ` など）。

| 系統 | メーカー | 親カテ | 子カテ |
|---|---|---|---|
| 前回と同じ（非通年） | ✅ | ✅ | ✅（v53 でキーにメーカー名を追加） |
| 記入以外前回と同じ（非通年） | ✅ | ✅ | ✅（**v53 で追加**） |
| 記入以外前回と同じ（通年） | ✅ | ✅（v51・v52） | ❌ 対応しない（制約） |

- ~~通年側は階層を一切見ておらず、提出ファイルに出てきた親カテゴリで代用している~~ → **v51・v52 で解消**。判定は `セール返答表-判定用-{メーカー,親カテ,子カテ}` の3本を使い、優先順位は **子カテ > 親カテ > メーカー**
- 通年／非通年の切り分けは設定シート A17:B42（名前定義 `通年セールメーカー`）

**外部ファイルのパスは設定シートに集約**されている（`Tconfig` テーブル、`Psalegoods_cs` などのパラメータクエリが参照）。M コードには `Pmaker` などのパラメータ名しか書かれておらず、実体は設定シートの C4 / B5:B15 / C16 セル。

そのため **Section1.m を読んでもパスは見えない**。逆に、シートの値は前回更新時の結果がキャッシュされているので、**外部ファイルが無くても件数などは openpyxl で確認できる**（`data_only=True`）。

## 作業上の注意

- **仕様が固まるまで実装しない。** ユーザーが自分で仕様を検討・決定する
- **回答は簡潔に。** 長い分析や表の羅列は避ける
- 選択肢を出すときは選択UIではなく文章で2〜3個に絞る
- xlsm を変更する場合は必ず**バージョンを上げて別ファイル保存**（v50 → v51）。既存ファイルは上書きしない
- git は NAS 上のため `safe.directory` の例外登録済み（グローバル設定）

## 既知の未対応課題

v51 仕様書 8章に記載。要点のみ。

- 非通年側（`ファイル前処理-編集用-親カテ抽出`）にも v51 と**同種の親カテゴリ欠落**がある（v53 で対応したのは子カテ階層であり、これは別件）
- 通年 sale 側が子カテ階層を見ない（v52 仕様書6章・v53 仕様書7章で「対応しない」と決定済み）
- Module3 と Module4 が完全に同一コードで、`tuunen` フラグ切り替えが無意味
- Module3/4 は最終行を A列基準で取得しているが出力は C〜G列。フィルタ非表示行も出力される
- 出力マクロがセルを1個ずつ読んでいて遅い（配列一括読み込みで大幅改善可能）
