# CLAUDE.md

このフォルダで作業する際の前提情報。

## プロジェクト概要

ZOZOTOWN のセール処理を行う Excel マクロブック `sale処理-v50.xlsm` の保守・改修。
メーカーから提出されたセール価格ファイルを Power Query で集約し、ZOZO へアップロードする TSV/TXT を VBA で出力する。

- **VBA**: 12モジュール（出力マクロ、集計、クエリ更新、重複チェック）
- **Power Query**: 約90クエリ（`Section1.m`）
- ブック本体は NAS 上（`T:` = `\\dop\ドキュメント`）

## 現在の状況（2026-08-03 時点）

**最新は `sale処理-v52.xlsm`。v51・v52 とも自社環境（NAS / 260731 データ）で検証合格。**

### v52：通年側の階層判定を統一（`仕様書_通年-階層優先順位の統一.md`）

判定用クエリ3本を新設し、優先順位を **子カテ > 親カテ > メーカー** に統一。3つの欠陥（親カテ階層を見ていない／記入以外側と返信あり側で順序が逆／①が親カテ行を無視）を解消。実データで実証済み。今のデータでは出力不変。

**既知の制約1件**（sale 側が子カテ階層を見ない）は対応せず記録。排他ルール違反時のみ発生し、重複チェックマクロで検出できる。仕様書6章。

### v51：親カテゴリ欠落バグの修正

- 新規3クエリ（①メーカー階層 ②親カテ階層 ③通年フィルタ付き統合）を追加し、`通年-記入以外前回と同じーsale` の Inner Join 参照先を1行差し替え
- EVER METAL 0→192 / BLACK JACKALL CROWN 0→123、既存3カテゴリは1行も変動なし（総行数 2,271→2,586）
- 全22シートの行単位 diff で「追加315行 / 削除0行」、VBA は全モジュール無変更
- VBA「ブランド品番重複チェック」も実行し **重複なし**（2026-08-03）

詳細は `仕様書_通年-記入以外前回と同じ_親カテゴリ欠落対応.md` を参照。再開時はまずこれを読むこと。

### 次にやること

1. **v53 問題2の実証テスト** — 仕様書 6.3（GERRY で他メーカー混入が消えるか）。問題1は実証済み
2. **F版を作って取引先環境で確認** — `作業手順_v51_親カテゴリ欠落修正.md` フェーズ2
3. 仕様書8章の他の既知課題（非通年側の同種欠落、Module3/4 のバグ、出力マクロの性能）

`25 nobu` による親カテ単位（②）のテストは 2026-08-03 に実施し合格（仕様書 10.7）。

## ファイル構成

| ファイル | 内容 |
|---|---|
| `sale処理-v50.xlsm` | 調査時点の本体（無変更で保持）。実体は **F版** — 第9章参照 |
| `sale処理-v51.xlsm` | 親カテゴリ欠落を修正（検証合格） |
| `sale処理-v52.xlsm` | 通年の階層判定を統一（検証合格） |
| `sale処理-v53.xlsm` | **最新**。非通年の子カテ階層に対応（出力不変を確認） |
| `saleファイルチェックーv9.xlsx` | ファイルチェック用 |
| `仕様書_通年-記入以外前回と同じ_親カテゴリ欠落対応.md` | 調査結果と修正仕様（v51・完了） |
| `仕様書_通年-階層優先順位の統一.md` | 階層判定を 子カテ>親カテ>メーカー に統一（v52・**実装済み／検証合格**） |
| `仕様書_非通年-子カテ階層の対応.md` | 非通年 記入以外の子カテ階層追加＋結合キー修正（v53・**実装済み**） |
| `_v52貼付用.txt` | v52 実装時の M コード貼付シート |
| `作業手順_v51_親カテゴリ欠落修正.md` | 実作業の手順書 |
| `設定シート_パス貼付用_260731.md` | 設定シートに貼る NAS パス一覧 |

### 無印版と F版

`sale処理-vNN.xlsm`（無印）と `vNNF.xlsm`（F付き）は、**設定シートのパスだけが違う別環境向け**のファイル。Power Query・VBA はほぼ同一。

| | 無印（自社 NAS） | F付き（取引先 Dropbox） |
|---|---|---|
| パス | `T:\…` / `\\dop\ドキュメント\…` | `C:\Users\user\Dropbox\…` |
| マスタ | 作業フォルダ内に全部コピー | Dropbox の別ツリーを参照 |

判別は設定シート C4 の先頭を見る。**同じ名前で保存しないこと**（260731 で取り違えが発生し、無印版が失われている）。

### 検証用データの場所

`..\260731\セール情報設定リスト202608019～202608079\セール情報設定リスト202608019～202608079\` に
`goods.csv`（124MB）、`salegoods.csv`、`売上目標実績2025.xlsx`、各提出フォルダが揃っている。
`◎セール返答表.xlsm` のみ `..\260731\◎セール返答表\` 配下。

**設定シートを NAS パスにすれば、この環境で更新・検証できる。**

## ブックの中身を調べる方法

xlsm を直接開かなくても、Python + oletools で中身を読める。**調査は必ずこの方法で行い、ブックを書き換えないこと。**

```bash
# 1. xlsm を zip として展開
Copy-Item sale処理-v50.xlsm sale.zip
Expand-Archive sale.zip -DestinationPath unz
```

**VBA の抽出**

```python
from oletools.olevba import VBA_Parser
p = VBA_Parser(r"unz\xl\vbaProject.bin")
for (fn, sn, vn, code) in p.extract_macros():
    print(vn, code)
```

**Power Query（M コード）の抽出**

```python
import re, base64, struct, zipfile, io
raw = open(r"unz\customXml\item1.xml", "rb").read().decode("utf-16")
m = re.search(r">([A-Za-z0-9+/=\s]+)</DataMashup>", raw)
b = base64.b64decode(m.group(1))
ver, plen = struct.unpack("<II", b[0:8])   # 先頭8バイトはヘッダー
z = zipfile.ZipFile(io.BytesIO(b[8:8+plen]))
z.extractall("pq")                          # pq/Formulas/Section1.m が全クエリのソース
```

**シートデータの確認**

`openpyxl.load_workbook(path, data_only=True, read_only=True)` で値を読む。

## 処理の全体像

```
メーカー提出ファイル（フォルダ別）      外部マスタ
  ├ 【記入以外前回と同じ】              ├ salegoods.csv  … 前回のセール価格
  ├ 【返信あり】                        ├ goods.csv      … 商品マスタ（子カテゴリを持つ）
  ├ 【通年セール】                      ├ メーカーマスタ … 親カテゴリ→メーカー名
  └ 【追加セール】                      └ ◎セール返答表  … メーカーごとのセール方針
        │                                      │
        └──→ fxファイル前処理 ←───────────────┘
                    │
              各シート（前処理／出力）
                    │
              VBA で TSV/TXT 出力 → ZOZO へアップロード
```

### 処理系統の意味（ユーザー確認済み・2026-08-03）

**セール価格設定リスト**（`00025-…様 セール価格設定リスト20260801～20260807.xlsx` 等）は
**毎回すべての取引先にメールしている**。系統の違いは、その後どうなったか。

そして**セール返答表は、どの系統でも担当者が設定する**。メーカーからの回答を担当者が記録する台帳であり、
返送の有無や回答の形（リスト返送／メールで一言／親カテ単位の指示）に関わらず、最後は担当者が埋める。
非表示行と担当者欄の「（保留）」、階層指定の排他ルール、前回の値の消し忘れ（34 湯浅札幌）は
いずれもこの入力作業の産物であり、だからこそ実装側にガードが要る。

| 系統 | リストの返送 | salegoods（前回価格） | セール対象 |
|---|---|---|---|
| **前回と同じ** | **返ってこない**（「前回と同じでお願いします」と回答があるだけ） | 全部これ | メーカーの商品**すべて** |
| **記入以外前回と同じ** | 返ってくる（価格を変える分だけ記入されている） | 未記入の商品を補完 | メーカーの商品**すべて** |
| **返信あり** | 返ってくる | **使わない** | **記入があった商品だけ** |

- 「前回と同じ」は返送が無いだけで、依頼はしている。提出フォルダに該当メーカーのファイルが
  無いのは**依頼していないからではなく、返ってこないから**
- **「記入以外前回と同じ」と「返信あり」の違いは、返送されたリストの未記入行の扱い。**
  前者は未記入分も前回価格でセール対象にする。後者は未記入分をセール対象にしない
- したがって**「返信あり」に salegoods 補完を入れてはいけない**。セール対象でない商品に
  価格が付いてしまう
- `Ptuunen-p` / `Ptuunen-zenkai` は通年セール価格の一覧であって、セール価格設定リストではない

この違いを踏まえないと除外処理の設計を誤る（v52 の検討時に実際に誤りかけた）。

### 重要な構造

**セール返答表は メーカー / 親カテ / 子カテ の3階層**で指示を出す。各階層に抽出クエリがある（`セール返答表-記入以外-メーカー`、`セール返答表-記入以外-親カテ` など）。

- 非通年側はメーカー階層・親カテ階層を実装済み
- **通年側は階層を一切見ておらず、提出ファイルに出てきた親カテゴリで代用している** ← バグの温床
- 通年／非通年の切り分けは設定シート A17:B42（名前定義 `通年セールメーカー`）

**外部ファイルのパスは設定シートに集約**されている（`Tconfig` テーブル、`Psalegoods_cs` などのパラメータクエリが参照）。M コードには `Pmaker` などのパラメータ名しか書かれておらず、実体は設定シートの C4 / B5:B15 / C16 セル。

そのため **Section1.m を読んでもパスは見えない**。逆に、シートの値は前回更新時の結果がキャッシュされているので、**外部ファイルが無くても件数などは openpyxl で確認できる**（`data_only=True`）。

## 作業上の注意

- **仕様が固まるまで実装しない。** ユーザーが自分で仕様を検討・決定する
- **回答は簡潔に。** 長い分析や表の羅列は避ける
- 選択肢を出すときは選択UIではなく文章で2〜3個に絞る
- xlsm を変更する場合は必ず**バージョンを上げて別ファイル保存**（v50 → v51）。既存ファイルは上書きしない
- git は NAS 上のため `safe.directory` の例外登録済み（グローバル設定）

## 既知の未対応課題

仕様書 8章に記載。要点のみ。

- 非通年側（`ファイル前処理-編集用-親カテ抽出`）にも同種の欠落がある
- Module3 と Module4 が完全に同一コードで、`tuunen` フラグ切り替えが無意味
- Module3/4 は最終行を A列基準で取得しているが出力は C〜G列。フィルタ非表示行も出力される
- 出力マクロがセルを1個ずつ読んでいて遅い（配列一括読み込みで大幅改善可能）
