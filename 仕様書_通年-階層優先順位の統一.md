# 仕様書：通年側の階層優先順位を統一する

- 対象ファイル：`sale処理-v51.xlsm` → **`sale処理-v52.xlsm`** として実装
- 作成日：2026-08-03
- ステータス：**仕様確定・実装未着手**
- 前提：`仕様書_通年-記入以外前回と同じ_親カテゴリ欠落対応.md`（v51 の修正）が適用済みであること

---

## 1. 背景

v51 の②（親カテ階層）のテスト中に、通年側の**指示の振り分け**に3つの欠陥が見つかった。いずれも根は同じで、「セール返答表の3階層をどの順で適用するか」が定まっていないこと。

| # | 欠陥 | 箇所 |
|---|---|---|
| 1 | **親カテ階層を見ていない**（メーカー階層と子カテ階層しか参照しない） | `通年-記入以外-前処理` |
| 2 | **記入以外側と返信あり側で優先順位が正反対** | `通年-記入以外-前処理` / `通年-返信あり-前処理` |
| 3 | メーカー行だけを見ており、親カテ行による上書きを考慮していない | `通年-記入以外-親カテ抽出-メーカー`（v51 で新設した①） |

### 1.1 欠陥2の詳細

同じ 【通年セール】 フォルダ（`Ptuunen`）の同じ行を、2本のクエリが逆のルールで振り分けている。

```m
// 通年-記入以外-前処理  → メーカー優先
each if [セール返答表-メーカー.最新] = null then [セール返答表.最新] else [セール返答表-メーカー.最新]

// 通年-返信あり-前処理  → 子カテ優先
each if [セール返答表.最新] <> null then [セール返答表.最新] else [セール返答表-メーカー.最新]
```

メーカー行と子カテ行の両方に指示があり、値が食い違うと:

| # | メーカー行 | 子カテ行 | 記入以外側の判定 | 返信あり側の判定 | 結果 |
|---|---|---|---|---|---|
| A | **記入以外前回と同じ** | **返信あり** | メーカーが非 null なので**記入以外 → 出力** | 子カテ優先で**返信あり → 出力** | **二重出力** |
| B | 返信あり | 記入以外前回と同じ | メーカーが null（下記）→ 子カテを見て記入以外 → 出力 | 子カテ優先で記入以外 → 除外 | 正しく動く |

**問題が起きるのはケースAだけ**で、原因は「記入以外側がメーカーを先に見ている」ことに尽きる。

ケースBでメーカー行が null になるのは、`通年-記入以外-前処理` が参照している `セール返答表-記入以外-メーカー` が**「最新」＝「記入以外前回と同じ」の行だけを抜き出したクエリ**だから。返答表の G列に `返信あり` と書かれていても、このクエリには載らないので LeftOuter 結合が空振りし、`[セール返答表-メーカー.最新]` が null になる。**返答表のセルが空という意味ではない。**

この絞り込みは結果的にケースBを救っているが、ケースAは救えない。決定事項4で絞り込み版の使用をやめるのは構造を揃えるためであり、**順序さえ直せば絞り込み版のままでも出力結果は同じ**になる（`= "記入以外前回と同じ"` で最終判定するため、null でも別の値でも除外される点は変わらない）。

### 1.2 今のデータで起きていない理由

260731 の返答表を全件確認したところ、**階層行がある10件すべてでメーカー行が空**だった。

| 階層行 | 件数 | メーカー行の指示 |
|---|---|---|
| 子カテ行（51 グルービー / BF2 select・SmileLife） | 2 | 空 |
| 親カテ行（09 ガレージ 4 / 19 湯浅 2 / 121 ジータ 2） | 8 | 空 |

「階層で指示を出すときはメーカー行を空にする」という運用で回避されているだけで、**返答表に前回の値が残っている程度のことで簡単に崩れる**。

---

## 2. 返答表の構造（確認済み）

| 列 | 内容 | 必須 |
|---|---|---|
| A | メーカー名 | **必須**（階層行でも必ず入れる運用） |
| B | 親カテ | 親カテ行のみ |
| C | 子カテ | 子カテ行のみ |
| G | 最新（指示） | — |

行の種別は B / C の埋まり方で決まる。260731 実データで検証済み（A列が空の階層行は0件、子カテ行はB列が空）。

| 種別 | B | C |
|---|---|---|
| メーカー行 | 空 | 空 |
| 親カテ行 | あり | 空 |
| 子カテ行 | 空 | あり |

**A列が必須**なので、階層の結合キーには必ずメーカー名を含める。

---

## 3. 決定事項

| # | 決定 | 根拠 |
|---|---|---|
| 1 | 優先順位は **子カテ > 親カテ > メーカー**（具体的なものが勝つ） | ユーザー決定（2026-08-03）。返信あり側が既にこの向き。子カテ単位の例外指定を効かせるにはこの順序でなければならない |
| 2 | 記入以外側・返信あり側の**両方**を同じ判定式に統一する | 片方だけ直すと欠陥2が残る |
| 3 | 結合キーには**メーカー名を含める** | A列必須のため。親カテ名・子カテ名の重複事故を防ぐ |
| 4 | 判定に使う返答表クエリは**「最新」で絞らないもの**を使う | 2本のクエリを同一構造にし、`処理方法` 列に実際の指示が入るようにするため。**欠陥2の原因ではない**（下記） |
| 5 | 通年 sale 側の**子カテ階層は引き続き対応しない** | v51 仕様書 4.3 決定事項2 を踏襲 |

---

## 4. 変更内容

Power Query のみ。VBA・シート構成・設定シートは変更しない。

### 4.1 新規クエリ：`セール返答表-親カテ`

既存の `セール返答表-メーカー`（メーカー行を全件）と対になるもの。**「最新」で絞らない**（決定事項4）。

```m
let
    ソース = セール返答表,
    フィルターされた行 = Table.SelectRows(ソース, each [親カテ] <> null and [子カテ] = null)
in
    フィルターされた行
```

**接続の作成のみ**にすること（v51 仕様書 10.6。シートとして読み込むと Module8 の行数集計が狂う）。

参考：既存の同種クエリ

| クエリ | 条件 | 「最新」で絞る |
|---|---|---|
| `セール返答表-メーカー` | 親カテ = null かつ 子カテ = null | しない |
| **`セール返答表-親カテ`**（新規） | 親カテ <> null かつ 子カテ = null | しない |
| `セール返答表-通年-子カテ` | 子カテ <> null | しない |

### 4.2 `通年-記入以外-前処理` の振り分けを差し替える

**差し替える範囲**：`マージされたクエリ数1` から `フィルターされた行` までの7ステップ。
**その前後（`ソース` と `#"トリム テキスト"` 以降）は変更しない。**

変更前:

```m
    マージされたクエリ数1 = Table.NestedJoin(ソース, {"メーカーマスタ.メーカー名"}, #"セール返答表-記入以外-メーカー", {"メーカー名"}, "セール返答表-メーカー", JoinKind.LeftOuter),
    #"展開された セール返答表-メーカー1" = Table.ExpandTableColumn(マージされたクエリ数1, "セール返答表-メーカー", {"最新"}, {"セール返答表-メーカー.最新"}),
    マージされたクエリ数 = Table.NestedJoin(#"展開された セール返答表-メーカー1", {"メーカーマスタ.メーカー名", "ブランド"}, #"セール返答表-通年-子カテ", {"メーカー名", "子カテ"}, "セール返答表", JoinKind.LeftOuter),
    #"展開された セール返答表" = Table.ExpandTableColumn(マージされたクエリ数, "セール返答表", {"最新"}, {"セール返答表.最新"}),
    追加されたカスタム = Table.AddColumn(#"展開された セール返答表", "カスタム", each if [#"セール返答表-メーカー.最新"] = null then [セール返答表.最新] else [#"セール返答表-メーカー.最新"]),
    #"名前が変更された列 " = Table.RenameColumns(追加されたカスタム,{{"カスタム", "処理方法"}}),
    フィルターされた行 = Table.SelectRows(#"名前が変更された列 ", each ([処理方法] = "記入以外前回と同じ")),
```

変更後:

```m
    J返答表メーカー = Table.NestedJoin(ソース, {"メーカーマスタ.メーカー名"}, #"セール返答表-メーカー", {"メーカー名"}, "返答表-メーカー", JoinKind.LeftOuter),
    E返答表メーカー = Table.ExpandTableColumn(J返答表メーカー, "返答表-メーカー", {"最新"}, {"返答表-メーカー.最新"}),
    J返答表親カテ = Table.NestedJoin(E返答表メーカー, {"メーカーマスタ.メーカー名", "親カテゴリー"}, #"セール返答表-親カテ", {"メーカー名", "親カテ"}, "返答表-親カテ", JoinKind.LeftOuter),
    E返答表親カテ = Table.ExpandTableColumn(J返答表親カテ, "返答表-親カテ", {"最新"}, {"返答表-親カテ.最新"}),
    J返答表子カテ = Table.NestedJoin(E返答表親カテ, {"メーカーマスタ.メーカー名", "ブランド"}, #"セール返答表-通年-子カテ", {"メーカー名", "子カテ"}, "返答表-子カテ", JoinKind.LeftOuter),
    E返答表子カテ = Table.ExpandTableColumn(J返答表子カテ, "返答表-子カテ", {"最新"}, {"返答表-子カテ.最新"}),
    #"名前が変更された列 " = Table.AddColumn(E返答表子カテ, "処理方法", each
        if [#"返答表-子カテ.最新"] <> null then [#"返答表-子カテ.最新"]
        else if [#"返答表-親カテ.最新"] <> null then [#"返答表-親カテ.最新"]
        else [#"返答表-メーカー.最新"]),
    フィルターされた行 = Table.SelectRows(#"名前が変更された列 ", each ([処理方法] = "記入以外前回と同じ")),
```

**最後の2ステップ名（`#"名前が変更された列 "` と `フィルターされた行`）は変えないこと。**後続の `#"トリム テキスト"` が `フィルターされた行` を参照している。`#"名前が変更された列 "` は末尾に半角スペースが1つ入る点に注意。

変更の要点は3つ。

1. 判定を **子カテ → 親カテ → メーカー** の順にする（欠陥2。**これが本質**）
2. **親カテ階層の結合を挟む**（欠陥1）
3. メーカー階層の参照先を `セール返答表-記入以外-メーカー`（絞り込み済み）から **`セール返答表-メーカー`（全件）** に変える（決定事項4。構造を揃えるためで、出力結果には影響しない）

### 4.3 `通年-返信あり-前処理` の振り分けを差し替える

4.2 とまったく同じ7ステップに置き換える。**最後の1行の条件だけが違う。**

```m
    フィルターされた行 = Table.SelectRows(#"名前が変更された列 ", each ([処理方法] = "返信あり")),
```

こちらも既存のステップ名（後続が参照しているもの）を壊さないこと。現行の該当箇所は `マージされたクエリ数` 〜 `フィルターされた行`。

### 4.4 `通年-記入以外-親カテ抽出-メーカー`（①）に除外を足す

メーカー行の指示で親カテゴリを拾う際、**その親カテゴリに専用の親カテ行がある場合はそちらが優先される**ため、①からは除外する（欠陥3）。

```m
let
    ソース = メーカーマスタ,
    対象メーカー = Table.NestedJoin(
        ソース, {"メーカー名"},
        #"セール返答表-記入以外-メーカー", {"メーカー名"},
        "返答表", JoinKind.Inner),
    親カテ指示を除外 = Table.NestedJoin(
        対象メーカー, {"メーカー名", "親カテゴリ名"},
        #"セール返答表-親カテ", {"メーカー名", "親カテ"},
        "親カテ行", JoinKind.LeftAnti),
    削除された他の列 = Table.SelectColumns(親カテ指示を除外, {"親カテゴリ名"}),
    削除された重複 = Table.Distinct(削除された他の列)
in
    削除された重複
```

追加したのは `親カテ指示を除外` の1ステップのみ。`セール返答表-親カテ` は「最新」で絞っていないので、**親カテ行に何らかの指示があれば内容を問わず除外**される。除外されたうえで、その親カテ行が「記入以外前回と同じ」なら②が拾い直す。

### 4.5 変更しないもの

- `通年-記入以外-親カテ抽出-親カテ`（②）と `通年-記入以外-親カテ抽出`（③）
- `通年-記入以外前回と同じーsale`、`通年-記入以外前回と同じ2`
- 非通年側すべて（`ファイル前処理-編集用` はフォルダで選別しており返答表を見ないため、この問題は起きない）
- `セール返答表-記入以外-メーカー` / `セール返答表-記入以外-親カテ` / `セール返答表-通年-子カテ`（既存のまま）
- 全 VBA モジュール

---

## 5. 検証

### 5.1 出力が変わらないことの確認（主たる合格条件）

260731 のデータには階層の競合が存在しない（1.2）ため、**この修正で出力は1行も変わらないはず**。変わったら、それは意図しない副作用。

v51（2026-08-03 11:38 更新、MD5 `4329D9B190A6A1DB3ED6F4EC2B23E4EE`）の実測値:

| シート | 行数 |
|---|---|
| 前回と同じ | 9,795 |
| 記入以外ー前処理 | 875 |
| 記入以外前回と同じ | 4,974 |
| 返信あり-前処理 | 6,032 |
| 返信あり | 5,932 |
| 通年-前回と同じ | 122 |
| **通年-記入以外-前処理** | **75** |
| **通年-記入以外前回と同じ** | **2,586** |
| **通年-返信あり-前処理** | **84** |
| **通年-返信あり** | **84** |
| 追加-前処理 / 追加セール | 1 / 1 |

`通年-記入以外前回と同じ` の親カテ内訳: MSA select 1,128 / Ree＆Mee 1,084 / EVER METAL 192 / BLACK JACKALL CROWN 123 / Diliraba 59。

あわせて v51 仕様書 10.5 と同じ**全シートの行単位 diff**（全列をタブ連結してソートし行単位で比較）を実施し、**追加0行 / 削除0行**を確認する。

### 5.2 欠陥が直ったことの確認（競合を作って試す）

`_テスト用\` に v52 と返答表のコピーを作り、返答表を次のように改変して確認する。

| # | 返答表の状態 | 期待する動作 |
|---|---|---|
| 1 | `25 nobu` メーカー行 = `記入以外前回と同じ`、かつ**親カテ行** `EVER METAL` = `返信あり` を追加 | EVER METAL が通年-記入以外から消え、通年-返信あり側で扱われる（親カテが勝つ） |
| 2 | `25 nobu` メーカー行を空にし、**親カテ行** `EVER METAL` = `記入以外前回と同じ` を追加。さらに提出ファイルの `NOB-575/583` に価格を記入 | **その記入行が前処理側で拾われる**（欠陥1が直った証拠。v51 では落ちる） |
| 3 | `51 グルービー` メーカー行 = `記入以外前回と同じ` を追加（子カテ行 `BF2 select` = `返信あり` はそのまま） | BF2 select は返信あり側だけに出る。**通年-記入以外と通年-返信あり で同じ品番が両方に出ない**（欠陥2が直った証拠。v51 では二重出力になる） |

**テスト2 は v51 で落ちることを先に確認してから v52 で拾えることを見る**と、修正の効果が明確になる。`NOB-575/583` は EVER METAL の300行目で、税込価格から設定に 2000 を入れれば記入行になる（2026-08-03 に一度実施し、元に戻してある）。

テスト3 は**シート間の品番重複**を見ないと検出できない。v51 仕様書 10.4 の検証項目7と同じ観点。

### 5.3 実施後

- 提出ファイル・本番の返答表を**必ず元に戻す**（テスト2で元データを触るため）
- 本番 v52 で「すべて更新」し、5.1 の値に戻ることを確認する

---

## 6. 対応しないもの

| # | 内容 | 理由 |
|---|---|---|
| 1 | 通年 sale 側の子カテ階層 | v51 仕様書 4.3 決定事項2 のとおり。子カテ単位の運用は縮小方向 |
| 2 | 非通年側の階層優先順位 | 非通年の前処理はフォルダで選別しており、返答表による振り分けを行わないため該当しない |
| 3 | 「前回と同じ」「追加セール」系統 | 今回の3欠陥はいずれも通年の記入以外／返信ありに限られる |
| 4 | v51 仕様書 8章の他の課題（Module3/4 のバグ、出力マクロの性能など） | 別件 |
