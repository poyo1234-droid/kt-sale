# 仕様書：通年側の階層判定を統一する

- 対象ファイル：`sale処理-v51.xlsm` → **`sale処理-v52.xlsm`**（作成済み・中身は未変更）
- 作成日：2026-08-03
- ステータス：**仕様確定・実装未着手**
- 前提：`仕様書_通年-記入以外前回と同じ_親カテゴリ欠落対応.md`（v51 の修正）が適用済みであること

---

## 1. 運用ルール（ユーザー確認済み）

セール返答表の指示は **メーカー単位 / 親カテ単位 / 子カテ単位のいずれか一つ**で出す。**混在させない**（排他）。

- 親カテ単位で出す場合、そのメーカーのメーカー行は**担当者が行ごと非表示にし、G列（最新）を空にする**（担当者欄も「（保留）」等になる）
- 対象外の行は今後**削除する方向**

### 1.1 実データでの裏付け（260731）

| メーカー | 行の状態 |
|---|---|
| 19 湯浅 | 27・29・31行が**非表示＋G列空**（担当「小林（保留）」）、28・30行が親カテ行で「前回と同じ」 |
| 09 ガレージ | 13・14・16行が**非表示＋G列空**、12・15・17・18行が親カテ行で「前回と同じ」 |
| 121 ジータ / 51 グルービー | メーカー行そのものが無く、階層行だけ |

**Power Query は非表示行も読むが、G列が空なので判定に影響しない。**列A（メーカー名）は必須で、実データでも空の階層行は0件だった。

### 1.2 行種別の判別

| 種別 | A（メーカー名） | B（親カテ） | C（子カテ） |
|---|---|---|---|
| メーカー行 | 必須 | 空 | 空 |
| 親カテ行 | 必須 | あり | 空 |
| 子カテ行 | 必須 | 空 | あり |

---

## 2. 直すべき欠陥

v51 の②のテスト中に3つ見つかった。**排他ルールを前提にすると、実害があるのは欠陥1だけ**で、2と3は同じ修正のついでに入る安全網。

| # | 欠陥 | 箇所 | 排他ルール下での位置づけ |
|---|---|---|---|
| **1** | **親カテ階層を見ていない** | `通年-記入以外-前処理` | **本丸。必ず直す**（下記 2.1） |
| 2 | 記入以外側と返信あり側で優先順位が正反対 | `通年-記入以外-前処理` / `通年-返信あり-前処理` | 保険（2.2） |
| 3 | メーカー行だけを見て親カテ行の上書きを考慮していない | `通年-記入以外-親カテ抽出-メーカー`（①） | 保険 |

### 2.1 欠陥1：排他ルールを守るほど確実に起きる

`通年-記入以外-前処理` はメーカー階層と子カテ階層しか参照していない。

排他ルールでは、親カテ単位で指示を出すとき**メーカー行のG列は空**になる。すると:

```
メーカー階層 → 空（ルールどおり）
子カテ階層   → 該当なし
親カテ階層   → 見ていない  ←←← ここ
⇒ 処理方法 = null ⇒ 除外される
```

**親カテ単位に「記入以外前回と同じ」を指定すると、その親カテゴリの提出ファイルに記入があった行が丸ごと落ちる。**ルール違反ではなく、ルールどおりに運用した結果として起きる。

なお v51 で直した sale 側（未記入＝前回と同じ で出す分）は③経由で正しく出るため、**記入した行だけが消える**という分かりにくい壊れ方になる。

### 2.2 欠陥2：混在したときだけ問題になる

```m
// 通年-記入以外-前処理  → メーカー優先
each if [セール返答表-メーカー.最新] = null then [セール返答表.最新] else [セール返答表-メーカー.最新]

// 通年-返信あり-前処理  → 子カテ優先
each if [セール返答表.最新] <> null then [セール返答表.最新] else [セール返答表-メーカー.最新]
```

メーカー行＝`記入以外前回と同じ`、子カテ行＝`返信あり` と混在した場合、記入以外側はメーカーを、返信あり側は子カテを採るため、**同じ記入行が両シートに出る（二重出力）**。

排他ルールが守られていれば起きないが、**指示の消し忘れで容易に発生する**（下記2.3の実例）。優先順位を揃えるコストはゼロなので直しておく。

### 2.3 メーカー行が重複しているケースがある

`34 湯浅札幌` はメーカー行が2行あり、**どちらも表示されていてG列に指示がある**。

| 行 | D（メール判別用） | G（最新） | 非表示 |
|---|---|---|---|
| 46 | 34 湯浅札幌 **4DIV** | 前回と同じ | いいえ |
| 47 | 34 湯浅札幌 **6DIV** | 前回と同じ | いいえ |

以前は親カテ単位で処理が異なっていたが今回は同じ処理になったため、**親カテ名を消したあとの非表示・削除の忘れ**と判断される（ユーザー確認済み）。

現状は両方とも同じ値なので出力は壊れていないが、**片方の値が変わるとメーカー名での結合が2件ヒットし、商品行が倍増する**。この種の残骸は現実に発生するため、実装側にガードを入れる（決定事項3）。

---

## 3. 決定事項

| # | 決定 | 根拠 |
|---|---|---|
| 1 | 通年の両前処理で**親カテ階層を参照する** | 欠陥1。本丸 |
| 2 | 判定順は **子カテ > 親カテ > メーカー**（具体的なものが勝つ）で両者を統一 | ユーザー決定（2026-08-03）。排他運用なら順序は結果に影響しないが、混在時の安全網になる |
| 3 | 判定に使う返答表は**「最新が空の行を除外」＋「キーで重複排除」**したものを使う | 2.3 の残骸対策。非表示行・削除忘れが読み込まれても行が増殖しない |
| 4 | 結合キーには**メーカー名を含める** | 列A必須のため。親カテ名・子カテ名だけで結合する事故を防ぐ |
| 5 | 通年 sale 側の**子カテ階層は引き続き対応しない** | v51 仕様書 4.3 決定事項2 を踏襲 |
| 6 | 既存の `セール返答表-メーカー` / `セール返答表-通年-子カテ` は**変更しない** | 他からの参照があるため。判定用は別クエリとして新設する |

---

## 4. 変更内容

Power Query のみ。VBA・シート構成・設定シートは変更しない。

### 4.1 新規クエリ3本（判定用）

いずれも **「最新」が空の行を除外**し、**キーで重複排除**する（決定事項3）。名前を `-判定用-` で揃え、既存クエリと区別する。

**すべて「接続の作成のみ」にすること**（v51 仕様書 10.6。シートとして読み込むと Module8 の行数集計が狂う）。

#### ① `セール返答表-判定用-メーカー`

```m
let
    ソース = セール返答表,
    メーカー行 = Table.SelectRows(ソース, each [親カテ] = null and [子カテ] = null and [最新] <> null),
    重複排除 = Table.Distinct(メーカー行, {"メーカー名"})
in
    重複排除
```

#### ② `セール返答表-判定用-親カテ`

```m
let
    ソース = セール返答表,
    親カテ行 = Table.SelectRows(ソース, each [親カテ] <> null and [子カテ] = null and [最新] <> null),
    重複排除 = Table.Distinct(親カテ行, {"メーカー名", "親カテ"})
in
    重複排除
```

#### ③ `セール返答表-判定用-子カテ`

```m
let
    ソース = セール返答表,
    子カテ行 = Table.SelectRows(ソース, each [子カテ] <> null and [最新] <> null),
    重複排除 = Table.Distinct(子カテ行, {"メーカー名", "子カテ"})
in
    重複排除
```

### 4.2 `通年-記入以外-前処理` の振り分けを差し替える

**差し替える範囲**：`マージされたクエリ数1` から `フィルターされた行` までの7ステップ。
**その前後（`ソース` と `#"トリム テキスト"` 以降）は変更しない。**

変更前:

```m
    マージされたクエリ数1 = Table.NestedJoin(ソース, {"メーカーマスタ.メーカー名"}, #"セール返答表-記入以外-メーカー", {"メーカー名"}, "セール返答表-メーカー", JoinKind.LeftOuter),
    #"展開された セール返答表-メーカー1" = Table.ExpandTableColumn(マージされたクエリ数1, "セール返答表-メーカー", {"最新"}, {"セール返答表-メーカー.最新"}),
    マージされたクエリ数 = Table.NestedJoin(#"展開された セール返答表-メーカー1", {"メーカーマスタ.メーカー名", "ブランド"}, #"セール返答表-通年-子カテ", {"メーカー名", "子カテ"}, "セール返答表", JoinKind.LeftOuter),
    #"展開された セール返答表" = Table.ExpandTableColumn(マージされたクエリ数, "セール返答表", {"最新"}, {"セール返答表.最新"}),
    追加されたカスタム = Table.AddColumn(#"展開された セール返答表", "カスタム", each if [#"セール返答表-メーカー.最新"] = null then [セール返答表.最新] else [#"セール返答表-メーカー.最新"]),
    #"名前が変更された列 " = Table.RenameColumns(追加されたカスタム,{{"カスタム", "処理方法"}}),
    フィルターされた行 = Table.SelectRows(#"名前が変更された列 ", each ([処理方法] = "記入以外前回と同じ")),
```

変更後:

```m
    J判定メーカー = Table.NestedJoin(ソース, {"メーカーマスタ.メーカー名"}, #"セール返答表-判定用-メーカー", {"メーカー名"}, "判定-メーカー", JoinKind.LeftOuter),
    E判定メーカー = Table.ExpandTableColumn(J判定メーカー, "判定-メーカー", {"最新"}, {"判定-メーカー.最新"}),
    J判定親カテ = Table.NestedJoin(E判定メーカー, {"メーカーマスタ.メーカー名", "親カテゴリー"}, #"セール返答表-判定用-親カテ", {"メーカー名", "親カテ"}, "判定-親カテ", JoinKind.LeftOuter),
    E判定親カテ = Table.ExpandTableColumn(J判定親カテ, "判定-親カテ", {"最新"}, {"判定-親カテ.最新"}),
    J判定子カテ = Table.NestedJoin(E判定親カテ, {"メーカーマスタ.メーカー名", "ブランド"}, #"セール返答表-判定用-子カテ", {"メーカー名", "子カテ"}, "判定-子カテ", JoinKind.LeftOuter),
    E判定子カテ = Table.ExpandTableColumn(J判定子カテ, "判定-子カテ", {"最新"}, {"判定-子カテ.最新"}),
    #"名前が変更された列 " = Table.AddColumn(E判定子カテ, "処理方法", each
        if [#"判定-子カテ.最新"] <> null then [#"判定-子カテ.最新"]
        else if [#"判定-親カテ.最新"] <> null then [#"判定-親カテ.最新"]
        else [#"判定-メーカー.最新"]),
    フィルターされた行 = Table.SelectRows(#"名前が変更された列 ", each ([処理方法] = "記入以外前回と同じ")),
```

**最後の2ステップ名（`#"名前が変更された列 "` と `フィルターされた行`）は変えないこと。**後続の `#"トリム テキスト"` が `フィルターされた行` を参照している。`#"名前が変更された列 "` は末尾に半角スペースが1つ入る点に注意。

親カテの結合キーが提出ファイル側は `親カテゴリー`（長音つき）、返答表側は `親カテ` である点にも注意。

### 4.3 `通年-返信あり-前処理` の振り分けを差し替える

4.2 とまったく同じ7ステップに置き換える。**最後の1行の条件だけが違う。**

```m
    フィルターされた行 = Table.SelectRows(#"名前が変更された列 ", each ([処理方法] = "返信あり")),
```

現行の該当箇所は `マージされたクエリ数` 〜 `フィルターされた行`。こちらも後続が参照しているステップ名を壊さないこと。

### 4.4 `通年-記入以外-親カテ抽出-メーカー`（①）に除外を足す

メーカー行の指示で親カテゴリを拾う際、**その親カテゴリに専用の親カテ行がある場合はそちらが優先される**ため除外する（欠陥3）。

```m
let
    ソース = メーカーマスタ,
    対象メーカー = Table.NestedJoin(
        ソース, {"メーカー名"},
        #"セール返答表-記入以外-メーカー", {"メーカー名"},
        "返答表", JoinKind.Inner),
    親カテ指示を除外 = Table.NestedJoin(
        対象メーカー, {"メーカー名", "親カテゴリ名"},
        #"セール返答表-判定用-親カテ", {"メーカー名", "親カテ"},
        "親カテ行", JoinKind.LeftAnti),
    削除された他の列 = Table.SelectColumns(親カテ指示を除外, {"親カテゴリ名"}),
    削除された重複 = Table.Distinct(削除された他の列)
in
    削除された重複
```

追加したのは `親カテ指示を除外` の1ステップのみ。`セール返答表-判定用-親カテ` は「最新」の値を問わないので、**親カテ行に何らかの指示があれば内容を問わず除外**される。除外されたうえで、その親カテ行が「記入以外前回と同じ」なら②が拾い直す。

### 4.5 変更しないもの

- `通年-記入以外-親カテ抽出-親カテ`（②）と `通年-記入以外-親カテ抽出`（③）
- `通年-記入以外前回と同じーsale`、`通年-記入以外前回と同じ2`、`通年-返信あり`
- 既存の `セール返答表-メーカー` / `セール返答表-通年-子カテ` / `セール返答表-記入以外-メーカー` / `セール返答表-記入以外-親カテ`（決定事項6）
- 非通年側すべて（`ファイル前処理-編集用` はフォルダで選別しており返答表を見ないため、この問題は起きない）
- 全 VBA モジュール

---

## 5. 検証

### 5.1 出力が変わらないことの確認（主たる合格条件）

260731 のデータには階層の混在が無い（1.1）ため、**この修正で出力は1行も変わらないはず**。変わったら意図しない副作用。

v51（2026-08-03 11:38 更新、MD5 `4329D9B190A6A1DB3ED6F4EC2B23E4EE`）の実測値:

| シート | 行数 |
|---|---|
| 前回と同じ | 9,795 |
| 記入以外ー前処理 | 875 |
| 記入以外前回と同じ | 4,974 |
| 返信あり-前処理 | 6,032 |
| 返信あり | 5,932 |
| 通年-前回と同じ | 122 |
| **通年-記入以外-前処理** | **75** |
| **通年-記入以外前回と同じ** | **2,586** |
| **通年-返信あり-前処理** | **84** |
| **通年-返信あり** | **84** |
| 追加-前処理 / 追加セール | 1 / 1 |

`通年-記入以外前回と同じ` の親カテ内訳: MSA select 1,128 / Ree＆Mee 1,084 / EVER METAL 192 / BLACK JACKALL CROWN 123 / Diliraba 59。

あわせて v51 仕様書 10.5 と同じ**全シートの行単位 diff**（全列をタブ連結してソートし行単位で比較）を実施し、**追加0行 / 削除0行**を確認する。

### 5.2 欠陥1が直ったことの確認（本丸）

`_テスト用\` に v52 と返答表のコピーを作って確認する。

1. 返答表コピーで `25 nobu` のメーカー行（37行）のG列を空にする（＝排他ルールどおりの状態）
2. 288行目に親カテ行を追加：`A=25 nobu / B=EVER METAL / C=空 / G=記入以外前回と同じ`
3. 提出ファイル `00025-NOBU総合企画…xlsx` の**300行目**（EVER METAL / `NOB-575/583`）の「税込価格から設定入力」に **2000** を入れる
4. 設定シート B7 をテスト用返答表に向けて、すべて更新

| | v51（修正前） | v52（修正後） |
|---|---|---|
| `NOB-575/583` が `通年-記入以外前回と同じ` に出るか | **出ない**（前処理で落ちる） | **出る**（税込2000で処理される） |
| `通年-記入以外-前処理` | 42行（MSA のみ） | **43行**（MSA 42 + EVER METAL 1） |
| EVER METAL の合計 | 192 | 192（sale 側が1件減り、前処理側が1件増える） |

**v51 で落ちることを先に確認してから v52 で拾えることを見る**と効果が明確になる。

### 5.3 欠陥2・3が直ったことの確認（安全網）

| # | 返答表の状態（混在させる＝ルール違反状態） | 期待する動作 |
|---|---|---|
| 1 | `25 nobu` メーカー行 = `記入以外前回と同じ` のまま、親カテ行 `EVER METAL` = `返信あり` を追加 | 親カテが勝ち、EVER METAL は通年-記入以外から消えて返信あり側で扱われる |
| 2 | `51 グルービー` にメーカー行 `記入以外前回と同じ` を追加（子カテ行 `BF2 select` = `返信あり` はそのまま） | 子カテが勝つ。**通年-記入以外と通年-返信ありに同じ品番が両方出ない**（v51 では二重出力になる） |

テスト2は**シート間の品番重複**を見ないと検出できない（v51 仕様書 10.4 の検証項目7と同じ観点）。

### 5.4 行の増殖が起きないことの確認

決定事項3のガードが効いているかを見る。`34 湯浅札幌` の46行・47行のどちらか一方のG列を別の値（例：`返信あり`）に変え、すべて更新しても**行数が増えないこと**を確認する。

※ 34 湯浅札幌は通年フォルダに無いため、非通年側（`記入以外前回と同じ` 4,974行）で確認する。

### 5.5 実施後

- 提出ファイル・本番の返答表を**必ず元に戻す**（5.2 で元データを触るため）
- 本番 v52 で「すべて更新」し、5.1 の値に戻ることを確認する

---

## 6. 対応しないもの

| # | 内容 | 理由 |
|---|---|---|
| 1 | 通年 sale 側の子カテ階層 | v51 仕様書 4.3 決定事項2 のとおり。子カテ単位の運用は縮小方向 |
| 2 | 非通年側の階層判定 | 非通年の前処理はフォルダで選別しており、返答表による振り分けを行わないため該当しない |
| 3 | 「前回と同じ」系統 | `通年-前回と同じ` は既にメーカー／親カテ／子カテの3本を Combine しており対応済み |
| 4 | 返答表の残骸行の掃除（34 湯浅札幌など） | 返答表側の運用で対処。今後は対象外行を削除する方針とユーザー確認済み |
| 5 | 参照元が無いクエリの削除（`セール返答表-通年-子カテ-返信あり`、`通年-記入以外-前処理－親カテ抽出`） | 動作に影響しない。v52 の検証が済むまで触らない |
