# 作業手順：v51 親カテゴリ欠落修正

仕様の根拠は `仕様書_通年-記入以外前回と同じ_親カテゴリ欠落対応.md` を参照。
パスの貼り付け値は `設定シート_パス貼付用_260731.md` を参照。

対象ファイル：`T:\10_業務資料\ファンアライブ\250829サーバー更新\251016-SALEデータ一式-今野様\エクセル開発\sale処理-v51.xlsm`

---

## 前提：v51 の素性

`sale処理-v51.xlsm` は `エクセル開発\sale処理-v50.xlsm` の複製（MD5 一致）。
そしてその v50 は **F版（Dropbox / `C:\Users\user\…`）**である。260731 フォルダで無印v50 が F版に置き換わっていたため。詳細は仕様書 第9章。

Power Query は無印版と F版で完全に同一（97クエリ、md5 一致）なので、**修正内容には影響しない**。違うのは設定シートのパスだけ。

したがってフェーズ1では、まず設定シートを NAS パスに戻す（＝無印版の復元）ところから始める。

---

## フェーズ1：このPC（NAS / 260731 データ）で修正と検証

RDP は不要。`260731` の作業フォルダに必要なデータが全部揃っている。

### 手順1-1　設定シートを NAS パスに戻す　✅ 完了（2026-08-02）

`sale処理-v50.xlsm` の設定シートを NAS パスに書き換え済み（MD5 `5B62ECE59C736614498ECD2BFCB65D1D`）。
これは 260731 で失われた無印v50（NAS版）の復元にあたる。同じ内容を v51 にもコピー済み。

以降の Power Query 修正は **v51 側で行う**。v50 はパス修正のみの無印NAS版として確定。

パスの検証結果（2026-08-02 実施）:

| セル | パラメータ | 結果 |
|---|---|---|
| C4 | 作業フォルダ | ✅ |
| B5 | `Psalegoods_cs` | ✅ salegoods.csv 4.6 MB |
| B6 | `Pmaker` | ✅ 売上目標実績2025.xlsx 2.4 MB |
| B7 | `Psalehentou` | ✅ ◎セール返答表 |
| B8 | `Psalekakaku` | ✅ 【記入以外前回と同じ】11項目 |
| B9 | `Pgoods` | ✅ goods.csv 121 MB |
| B10 | `Ptuunen` | ✅ 【通年セール】4項目 |
| B11 | `Ptuika` | ✅ 【追加セール】1項目 |
| B12 | `Phensin` | ✅ 【返信あり】43項目 |
| B13 | `Ptuunen-p` | ✅ 3項目 |
| **B14** | `Pgoods_cs`（設定不要） | ❌ フォルダなし → **問題なし**（下記） |
| B15 | `Ptuunen-zenkai` | ✅ 2項目 |
| C16 | （通年セール表） | ✅ |

セール期間 A2 / B2 = `2026080109` / `2026080709` も回次と一致。

**B14 は放置してよい。** `Pgoods_cs` パラメータを参照しているクエリが1本も存在しないことを Section1.m で確認済み（名前の似た `Psalegoods_cs` は別物で、B5 を参照しており正常）。

### 手順1-2　修正前のベースラインを取る　✅ 完了（2026-08-02 22:42）

NAS パスで「すべて更新」→ 上書き保存を実施（v51 MD5 `D400938D49D9F96A601A673DD50928F3`）。

**F版が Dropbox 環境で 7/31 に保存した値と1行も違わなかった。** 260731 のデータは 7/31 時点と同一で、環境移行は正しく行えている。

| 項目 | 参考値（F版7/31） | **NAS 実測** | |
|---|---|---|---|
| 「通年-記入以外前回と同じ」総行数 | 2,271 | **2,271** | 一致 |
| └ MSA select | 1,128 | **1,128** | 一致 |
| └ Ree＆Mee | 1,084 | **1,084** | 一致 |
| └ Diliraba | 59 | **59** | 一致 |
| └ **EVER METAL** | 0 | **0** | **バグ再現** |
| └ **BLACK JACKALL CROWN** | 0 | **0** | **バグ再現** |
| 「通年-記入以外-前処理」総行数 | 75 | **75** | 一致（MSA 42 / Ree＆Mee 19 / Diliraba 14） |
| 「記入以外前回と同じ」（非通年）総行数 | ― | **4,974** | 新規取得 |

「重複」列と品番のベースライン:

| シート | 「重複」非空 | ブランド品番の重複 | その他 |
|---|---|---|---|
| 通年-記入以外前回と同じ | **0 件** | **0 種**（2,271 全てユニーク） | 「要確認」「確認事項」とも 0 件 |
| 記入以外前回と同じ | **0 件** | **0 種**（4,974 全てユニーク） | 「要確認」4件（削除対象3 / １％処理1）、「属性」2件（要確認2） |

検証項目4（重複が増えていないこと）は **0 のままであること**が合格条件。
検証項目5（非通年が変化しないこと）は **4,974 のままであること**が合格条件。

非通年の親カテゴリ内訳（参考）:
A＋SELECT 1,833 / Gloria 1,026 / CharaBasket 636 / SOG SELECT 439 / 1004ST 220 /
WINDS select 219 / LION HEART 201 / fetchcollection 200 / D SELECT 153 / attiree 24 / B-Style 23

### 手順1-3　新規クエリを3本作る

データ → クエリと接続 → 新しいクエリ → その他のソース → 空のクエリ → 詳細エディター。
クエリ名は下記の見出しどおりに設定すること（後続クエリが名前で参照するため）。

列名は v51 の Section1.m で照合済み。そのまま貼れる。

#### ① `通年-記入以外-親カテ抽出-メーカー`

```m
let
    ソース = メーカーマスタ,
    対象メーカー = Table.NestedJoin(
        ソース, {"メーカー名"},
        #"セール返答表-記入以外-メーカー", {"メーカー名"},
        "返答表", JoinKind.Inner),
    削除された他の列 = Table.SelectColumns(対象メーカー, {"親カテゴリ名"}),
    削除された重複 = Table.Distinct(削除された他の列)
in
    削除された重複
```

#### ② `通年-記入以外-親カテ抽出-親カテ`

```m
let
    ソース = #"セール返答表-記入以外-親カテ",
    削除された他の列 = Table.SelectColumns(ソース, {"親カテ"}),
    名前変更 = Table.RenameColumns(削除された他の列, {{"親カテ", "親カテゴリ名"}}),
    削除された重複 = Table.Distinct(名前変更)
in
    削除された重複
```

#### ③ `通年-記入以外-親カテ抽出`

```m
let
    結合 = Table.Combine({
        #"通年-記入以外-親カテ抽出-メーカー",
        #"通年-記入以外-親カテ抽出-親カテ"}),
    削除された重複 = Table.Distinct(結合),
    通年のみ = Table.NestedJoin(削除された重複, {"親カテゴリ名"},
        通年セールメーカー, {"親カテゴリ"},
        "通年", JoinKind.Inner),
    削除された他の列 = Table.SelectColumns(通年のみ, {"親カテゴリ名"})
in
    削除された他の列
```

`通年のみ` のステップを省くと、セール返答表でメーカー単位に指定されたメーカーの**非通年の親カテゴリまで**通年シートに流れ込み、通年・非通年で 4,838 件の二重出力が起きる（仕様書 10.1）。必ず入れること。

**3本とも必ず「接続の作成のみ」にすること。** シートとして読み込むとシートが3枚増え、Module8（行数集計）が全ワークシートを巡回して合計するため「行数の合計」が実際より多くなる（仕様書 10.6）。

すでにシートとして読み込んでしまった場合は、クエリと接続ペインで該当クエリを右クリック → **読み込み先** → **接続の作成のみ** → OK。確認ダイアログで OK を押さないと反映されない。**変更後は必ず Ctrl+S で保存する。**

**ここで③のプレビューに `EVER METAL` と `BLACK JACKALL CROWN` が入っていることを確認する。** 入っていなければ以降に進んでも直らない。

### 手順1-4　既存クエリを1行だけ差し替える

`通年-記入以外前回と同じーsale` を詳細エディターで開く。

変更前（4行目）:

```m
    マージされたクエリ数 = Table.NestedJoin(削除された列, {"親カテゴリ"}, #"通年-記入以外-前処理－親カテ抽出", {"親カテゴリー"}, "ファイル前処理-編集用-親カテ抽出", JoinKind.Inner),
```

変更後:

```m
    マージされたクエリ数 = Table.NestedJoin(削除された列, {"親カテゴリ"}, #"通年-記入以外-親カテ抽出", {"親カテゴリ名"}, "親カテ抽出", JoinKind.Inner),
```

他の行は触らない。`通年-記入以外-前処理－親カテ抽出` は参照されなくなるが**削除しない**。

### 手順1-5　検証

データ → すべて更新。

| # | 確認内容 | 期待結果 | 自社環境の実測 |
|---|---|---|---|
| 1 | `EVER METAL` が出力される | 出力あり | ✅ 192 |
| 2 | `BLACK JACKALL CROWN` が出力される | 出力あり | ✅ 123 |
| 3 | Ree＆Mee / MSA select / Diliraba の件数 | 手順1-2 から**減っていない** | ✅ 1,084 / 1,128 / 59 |
| 4 | 「重複」列 | 手順1-2 と比べて増えていない | ✅ 0 のまま |
| 5 | 「記入以外前回と同じ」（非通年）総行数 | 手順1-2 から**変化なし** | ✅ 4,974 |
| **7** | **通年・非通年で同じブランド品番が両方に出ていないか** | **0件** | ✅ 0 |
| **8** | **通年シートの親カテゴリが設定シートの通年セール表に全て含まれるか** | 全て含まれる | ✅ |
| 6 | VBA「ブランド品番重複チェック」 | 新たな重複なし | ⬜ 未実施 |

**項目7・8 は当初の検証項目に無かったが、必ず実施すること。** 項目4・5 は各シート内しか見ないため、通年・非通年の二重出力を検出できない。実際、③の `通年のみ` ステップを入れる前は 4,838 件の二重出力があったが、項目1〜6 では全て合格と出てしまった（仕様書 10.1）。

自社環境の結果: 通年シート総行数 2,271 → **2,586**（増加は EVER METAL 192 + BLACK JACKALL CROWN 123 = 315 行ちょうど）。
検証時の v51: MD5 `C9852737704E30C69ACC7DDCEC668ECF`。

すべて OK なら v51 を保存し、git にコミットする。

---

## フェーズ2：取引先の環境（Dropbox / F版）で確認

自社検証が通ったら、F版として取引先環境用のパスに差し替えたものを用意する。

### 手順2-1　v51F を作る

v51 をコピーして `sale処理-v51F.xlsm` として保存する。
**無印と F版を同じ名前で保存しないこと**（260731 で起きた取り違えの再発防止）。

### 手順2-2　設定シートを Dropbox パスに戻す

F版のパスは以下（`エクセル開発\sale処理-v50.xlsm` にもともと入っていた値）。

| セル | 値 |
|---|---|
| C4 | `C:\Users\user\Dropbox\ZOZOTOWN-DB\1 ZOZOTOWN\【ZOZO_セール】\セール情報設定リスト202608019～202608079\` |
| B5 | `…\セール情報設定リスト202608019～202608079\salegoods.csv` |
| B6 | `C:\Users\user\Dropbox\営業リスト\売上目標実績2025.xlsx` |
| B7 | `C:\Users\user\Dropbox\ZOZOTOWN-DB\1 ZOZOTOWN\【ZOZO_セール】\◎セール返答表.xlsm` |
| B8 | `…\セール情報設定リスト202608019～202608079\【記入以外前回と同じ】` |
| B9 | `…\セール情報設定リスト202608019～202608079\goods.csv` |
| B10 | `…\セール情報設定リスト202608019～202608079\【通年セール】(メンズサンエー,グルービー,フェリック)` |
| B11 | `…\セール情報設定リスト202608019～202608079\【追加セール】` |
| B12 | `…\セール情報設定リスト202608019～202608079\【返信あり】` |
| B13 | `…\セール情報設定リスト202608019～202608079\・通年セール価格ー返信あり・記入以外同じ` |
| B14 | `…\セール情報設定リスト202608019～202608079\設定不要` |
| B15 | `…\セール情報設定リスト202608019～202608079\・通年セール価格ー前回と同じ` |
| C16 | `C:\Users\user\Dropbox\ZOZOTOWN-DB\1 ZOZOTOWN\【ZOZO_セール】\セール情報設定リスト202608019～202608079\` |

F版は無印と違い、**メーカーマスタと返答表が作業フォルダの外**（Dropbox の別ツリー）にある。

### 手順2-3　パスが通るか確認

取引先PCで、エクスプローラーのアドレスバーに `%USERPROFILE%` と入力して Enter。

- `C:\Users\user` が開く → 上表のままで動く
- 別のパスが開く → ユーザー名部分を実際のものに置換する

あわせて Dropbox の同期が完了していることを確認する。クラウド専用のプレースホルダ状態だと Power Query が読めない。

### 手順2-4　検証

手順1-5 と同じ6項目。件数の基準値は環境ごとに違うので、**その環境での修正前の実測を先に控えてから**比較する。

確認の本質は2点。

- EVER METAL / BLACK JACKALL CROWN のように、1件も記入がない親カテゴリが出力されること
- 既存カテゴリの件数が減っていないこと

---

## トラブル時

| 症状 | 原因の可能性 |
|---|---|
| 「データソースが見つかりません」 | パス不一致。Dropbox 版なら同期未完了も疑う |
| 更新が終わらない | `goods.csv` が 124 MB。NAS 経由だと特に遅い。強制終了しない |
| ③のプレビューに対象親カテが出ない | セール返答表の「最新」列が「記入以外前回と同じ」になっていない／メーカーマスタに該当メーカーの行がない |
| 既存カテゴリの件数が減った | 5.4 の差し替えで結合キー（`親カテゴリ名`）を間違えた可能性 |
| 重複が増えた | 通年・非通年の二重出力。設定シートの通年セール表とセール返答表の指示が食い違っていないか確認 |

問題が起きたら v51 を保存せずに閉じれば元に戻せる。`sale処理-v50.xlsm` は一切変更していない（git にコミット済み、blob `9ff727d`）。
