# 仕様書：「通年-記入以外前回と同じ」親カテゴリ欠落の修正

- 対象ファイル：`sale処理-v50.xlsm`
- 作成日：2026-08-02
- ステータス：**仕様確定・未実装**（コード変更は未実施）

---

## 1. 現象

シート「通年-記入以外前回と同じ」で、データ → すべて更新を実行しても、以下の親カテゴリの商品が1件も出力されない。

- `EVER METAL`
- `BLACK JACKALL CROWN`

いずれも設定シートの通年セール表（A17:B42）に登録済み、メーカーは `25 nobu`。

### 実データによる確認結果

| 対象 | 内容 |
|---|---|
| 設定シートの通年セール対象 | BF2 select / TRUSS / MSA select / Ree＆Mee / **BLACK JACKALL CROWN** / Diliraba / **EVER METAL** |
| 「通年-記入以外-前処理」の中身（全75行） | MSA select 42行 / Diliraba 14行 / Ree＆Mee 19行 → **上記2つは 0行** |
| 「通年-記入以外前回と同じ」の出力（2,271行） | Ree＆Mee 1,084 / MSA select 1,128 / Diliraba 59 → **上記2つは 0件** |

なお、セール返答表の設定自体は正常だった（`25 nobu` はメーカー行で「記入以外前回と同じ」を指示済み）。返答表側の問題ではない。

---

## 2. 原因

`fxファイル前処理` の以下のフィルタが起点。

```m
フィルターされた行 =
    Table.SelectRows(挿入された結合列, each ([結合済み] <> "")),
```

`結合済み` は「オフ率 入力」「値引き金額 入力」「税込価格から設定 入力」の3列を連結した値。つまり **メーカーが1つも記入しなかった行はここで除去される**（この動作自体は正しい）。

問題は、その**フィルタ後のデータから親カテゴリ一覧を作っている**こと。

```
fxファイル前処理  ← 記入なしの行を除去
  └ 通年-記入以外-前処理                 ← EVER METAL の行が 0件 になる
      └ 通年-記入以外-前処理－親カテ抽出    ← 親カテ一覧に EVER METAL が載らない
          └ 通年-記入以外前回と同じーsale
              Table.NestedJoin(…, {"親カテゴリ"}, …, JoinKind.Inner)  ← Inner なので 0件
                  └ 通年-記入以外前回と同じ   ← 親カテゴリごと丸ごと欠落
```

**「その親カテゴリに1件も記入がない」＝「全商品が前回と同じ」という、本来もっとも出力されるべきケースで、親カテゴリが丸ごと消える。**

記入が1件でもあるかどうかだけで、その親カテゴリ全体の出力有無が決まる。Diliraba は14行だけ記入があったため親カテ一覧に載り、59件出力されていた。

---

## 3. 構造上の背景

セール返答表は **メーカー / 親カテ / 子カテ の3階層**で指示を出す設計。各階層に対応する抽出クエリが用意されている。

| | メーカー単位 | 親カテ単位 | 子カテ単位 |
|---|---|---|---|
| 非通年「記入以外前回と同じ」 | ✅ `salegoods-記入以外-メーカー` | ✅ `salegoods-記入以外-親カテ` | ❌ なし |
| 非通年「前回と同じ」 | ✅ `salegoods-前回と同じ-メーカー` | ✅ `salegoods-前回と同じ-親カテ` | ✅ `salegoods-前回と同じ-子カテ` |
| **通年「記入以外前回と同じ」** | ❌ | ❌ | ❌ |

非通年側は階層ごとにクエリを分けて Combine している。

```m
// ファイル前処理-編集用-アップシート
追加されたクエリ1 = Table.Combine({前処理, #"salegoods-記入以外-メーカー"}),
追加されたクエリ2 = Table.Combine({追加されたクエリ1, #"salegoods-記入以外-親カテ"}),
```

対して**通年側は返答表の階層を一切見ておらず、「提出ファイルに実際に出てきた親カテゴリ」で代用**している。今回の欠落の本質はここにある。

---

## 4. 修正仕様（確定）

### 4.1 基本方針

親カテゴリ一覧の取得元を、**提出ファイル → メーカーマスタ＋セール返答表** に変更する。

提出ファイルに依存しないため、メーカーが該当行を削除して提出してきた場合でも欠落しない。

### 4.2 階層対応

通年側を**メーカー階層＋親カテ階層の2本立て**にする（非通年側と同じ構造に揃える）。

| 階層 | 対応 | 参照する既存クエリ |
|---|---|---|
| メーカー単位 | **対応する** | `セール返答表-記入以外-メーカー` |
| 親カテ単位 | **対応する** | `セール返答表-記入以外-親カテ` |
| 子カテ単位 | **対応しない** | — |

### 4.3 決定事項と根拠

| # | 決定 | 根拠 |
|---|---|---|
| 1 | 設定シートの通年セール表による絞り込みは**行わない** | メーカーマスタの親カテゴリと設定シートの親カテゴリが一致しているとユーザー確認済み。よって通年・非通年の重複は発生しない。設定シートへの手入力も不要になる |
| 2 | 子カテ階層は**対応しない** | 子カテ単位の運用は現時点のみで、今後はメーカー単位＋親カテ単位に移行するため |
| 3 | `fxファイル前処理` のフィルタは**変更しない** | 記入がある行だけを処理対象にする設計自体は正しい。他の出力への影響を避ける |
| 4 | `通年-記入以外-前処理` 本体は**変更しない** | 記入行のみを扱うという役割は正しい |

---

## 5. 変更内容

変更は Power Query のみ。VBA・シート構成・設定シートには手を入れない。

### 5.1 新規クエリ①：`通年-記入以外-親カテ抽出-メーカー`

メーカー行で「記入以外前回と同じ」が指示されたメーカーの、全親カテゴリを取得する。

```m
let
    ソース = メーカーマスタ,
    対象メーカー = Table.NestedJoin(
        ソース, {"メーカー名"},
        #"セール返答表-記入以外-メーカー", {"メーカー名"},
        "返答表", JoinKind.Inner),
    削除された他の列 = Table.SelectColumns(対象メーカー, {"親カテゴリ名"}),
    削除された重複 = Table.Distinct(削除された他の列)
in
    削除された重複
```

`メーカーマスタ` は親カテゴリ名でユニーク化済み（`Table.Distinct(変更された型, {"親カテゴリ名"})`）のため、メーカー名で絞れば当該メーカーの全親カテゴリが漏れなく取れる。

### 5.2 新規クエリ②：`通年-記入以外-親カテ抽出-親カテ`

親カテ行で「記入以外前回と同じ」が指示された親カテゴリを取得する。

```m
let
    ソース = #"セール返答表-記入以外-親カテ",
    削除された他の列 = Table.SelectColumns(ソース, {"親カテ"}),
    #"名前が変更された列 " = Table.RenameColumns(削除された他の列, {{"親カテ", "親カテゴリ名"}}),
    削除された重複 = Table.Distinct(#"名前が変更された列 ")
in
    削除された重複
```

### 5.3 新規クエリ③：`通年-記入以外-親カテ抽出`

①と②を統合した最終的な親カテゴリ一覧。

```m
let
    結合 = Table.Combine({
        #"通年-記入以外-親カテ抽出-メーカー",
        #"通年-記入以外-親カテ抽出-親カテ"}),
    削除された重複 = Table.Distinct(結合)
in
    削除された重複
```

### 5.4 既存クエリの修正：`通年-記入以外前回と同じーsale`

Inner Join の参照先を差し替える（1行のみ）。

```m
// 変更前
マージされたクエリ数 = Table.NestedJoin(削除された列, {"親カテゴリ"},
    #"通年-記入以外-前処理－親カテ抽出", {"親カテゴリー"},
    "ファイル前処理-編集用-親カテ抽出", JoinKind.Inner),

// 変更後
マージされたクエリ数 = Table.NestedJoin(削除された列, {"親カテゴリ"},
    #"通年-記入以外-親カテ抽出", {"親カテゴリ名"},
    "親カテ抽出", JoinKind.Inner),
```

### 5.5 影響を受けないもの

- `通年-記入以外-前処理－親カテ抽出`：参照されなくなるが**削除しない**（他からの参照有無を「クエリの依存関係」で確認するまで残す）
- `fxファイル前処理`、`通年-記入以外-前処理`、`通年-記入以外-前処理-ブランド品番`
- 重複排除の LeftAnti（提出ファイルに記入済みの商品を sale 側から除外する処理）は現状のまま機能する
- 全 VBA モジュール

---

## 6. 適用手順

1. `sale処理-v50.xlsm` をコピーし、`sale処理-v51.xlsm` として保存する（v50 は変更しない）
2. データ → クエリと接続 → 新しいクエリ → 詳細エディター で、5.1〜5.3 の3クエリを作成する
3. `通年-記入以外前回と同じーsale` を詳細エディターで開き、5.4 の1行を差し替える
4. データ → すべて更新
5. 検証（第7章）

---

## 7. 検証項目

| # | 確認内容 | 期待結果 |
|---|---|---|
| 1 | 「通年-記入以外前回と同じ」に `EVER METAL` が出力されるか | 出力される |
| 2 | 同シートに `BLACK JACKALL CROWN` が出力されるか | 出力される |
| 3 | 既存3カテゴリの件数 | Ree＆Mee 1,084 / MSA select 1,128 / Diliraba 59 から**減っていない**こと |
| 4 | 「重複」列 | 修正前と比べて重複が増えていないこと |
| 5 | 「記入以外前回と同じ」（非通年）シートの件数 | 修正前から**変化しない**こと（通年・非通年の二重出力が起きていない） |
| 6 | VBA「ブランド品番重複チェック」の実行結果 | 新たな重複が検出されないこと |

修正前の件数を控えてから実施すること。

---

## 8. 今回対応しない既知の課題

以下は本件とは別に確認済みの問題。今回の修正には含めない。

### 8.1 非通年側にも同じ欠落がある

`ファイル前処理-編集用-親カテ抽出`（Section1.m 625行）も、`ファイル前処理-編集用`（＝フィルタ後）から親カテゴリを抽出しており、構造は同一。「記入以外前回と同じ」シートでも同じ親カテゴリ単位の欠落が起きうる。

ただし非通年側は `salegoods-記入以外-メーカー` と `salegoods-記入以外-親カテ` で別途 Combine しているため、欠落の影響範囲は通年側より小さい。

### 8.2 VBA の不具合

| # | 箇所 | 内容 |
|---|---|---|
| 1 | Module3 / Module4 | `ExportColumnsAEInTXTChunks_ANSI` と `..._tuunen` が**完全に同一のコード**。設定シートの `tuunen` フラグ切り替えが実質無意味 |
| 2 | Module3 / Module4 | 最終行を**A列基準**で取得しているが、出力は C〜G列。A列が空だと出力が途切れる |
| 3 | Module3 / Module4 | **フィルタで非表示の行も出力**される（Module5 / Module7 は対応済み） |
| 4 | Module2 | 全列が空の行で `Left(outputLine, -1)` となり実行時エラー |
| 5 | Module10 | `CleanExit` ラベルに飛ぶ経路がなく、クエリ未発見時も「完了しました」と表示される |

### 8.3 VBA の性能

全出力マクロがセルを1個ずつ読んでいる（`ws.Cells(j, k).Value`）。最大シートは内部データ 7.8MB 規模。配列一括読み込みに変更すれば大幅に高速化できる。Module8 の行数カウントも1行ずつ `CountIf` を呼んでおり重い。

---

## 付録：調査手法

このブックの中身を外部から確認する方法（Python + oletools が必要）。

**VBA の抽出**

```python
from oletools.olevba import VBA_Parser
p = VBA_Parser(r"xl\vbaProject.bin")   # xlsm を zip として展開して取得
for (fn, sn, vn, code) in p.extract_macros():
    ...
```

**Power Query（M コード）の抽出**

`customXml/item1.xml` の DataMashup を base64 デコード → 先頭8バイト（version 4byte, length 4byte）を読み飛ばす → 続きが zip なので展開 → `Formulas/Section1.m` が全クエリのソース。
